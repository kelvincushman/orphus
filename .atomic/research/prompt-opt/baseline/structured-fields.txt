# command: bash .atomic/research/prompt-opt/baseline/structured-fields-rerun.sh > .atomic/research/prompt-opt/baseline/structured-fields.txt

## stop_review_loop
packages/workflows/builtin/goal-prompts.ts:169:        "- Completion transition: the orchestrator may claim readiness, but reviewer quorum plus the deterministic reducer decides final workflow status. Each reviewer's stop_review_loop boolean is the single authoritative approval signal; the run completes when the quorum of reviewers independently report stop_review_loop=true.",
packages/workflows/builtin/goal-prompts.ts:237:            "If the repository state satisfies every implementation and validation requirement and only PR/MR/review creation remains, approve the implementation: set goal_oracle_satisfied=true, stop_review_loop=true, no blocking findings, and note the PR as the remaining final action rather than an implementation gap.",
packages/workflows/builtin/goal-prompts.ts:296:        "If no finding clears this bar and receipts prove the objective, return an empty findings array, mark the patch correct, set goal_oracle_satisfied true, and set stop_review_loop true.",
packages/workflows/builtin/goal-prompts.ts:339:        "7. Decide whether the receipt/evidence map proves completion; if an applicable material probe or other evidence is uncertain, indirect, stale, missing, blocked, failed, or narrower than the requested outcome, use the existing traceability/error/finding fields, set goal_oracle_satisfied=false, and set stop_review_loop=false.",
packages/workflows/builtin/goal-prompts.ts:340:        "8. If tools or dependencies prevent necessary verification after reasonable recovery, populate reviewer_error and set stop_review_loop=false rather than approving around the limitation.",
packages/workflows/builtin/goal-prompts.ts:347:        "If the strict blocked audit is satisfied by current evidence, do not invent a finding. Set stop_review_loop=false, goal_oracle_satisfied=false, verification_remaining to the concise blocker, and reviewer_error.kind to dependency_unavailable or tool_failure with reviewer_error.message set to the same concise blocker.",
packages/workflows/builtin/goal-prompts.ts:368:        "Always return requirements_traceability as a non-empty array that enumerates every explicit objective and acceptance-criteria clause. Traceability and findings are audit evidence for humans and later stages; the harness gates approval on your stop_review_loop boolean alone, so derive that flag from them carefully.",
packages/workflows/builtin/goal-prompts.ts:369:        "When setting stop_review_loop=true, every implementation/validation requirements_traceability entry must be proven, goal_oracle_satisfied must be true, verification_remaining must say no objective-relevant implementation or validation remains, and reviewer_error must be null or omitted.",
packages/workflows/builtin/goal-prompts.ts:370:        "Goal-specific pre-verdict self-audit: before stop_review_loop=true, confirm goal_oracle_satisfied is true and verification_remaining reports no objective-relevant verification gap, in addition to the correctness, traceability, findings, applicable-risk evidence, and reviewer-error checks in independent_verification.",
packages/workflows/builtin/goal-prompts.ts:371:        "Clauses that only the workflow process can satisfy — reviewer quorum/approval-count clauses, and (when create_pr is enabled) the post-approval PR/MR/review creation final action — are never implementation gaps: record them as final-action/process items and do not let them hold stop_review_loop at false.",
packages/workflows/builtin/goal-prompts.ts:372:        "If you hit a reviewer/tool/validation error, set stop_review_loop=false and populate reviewer_error instead of pretending the patch is approved.",
packages/workflows/builtin/goal-prompts.ts:378:        "stop_review_loop is the single authoritative convergence flag: the harness approves this review exactly when stop_review_loop=true and reviewer_error is null/omitted, without recomputing approval from findings or traceability.",
packages/workflows/builtin/goal-prompts.ts:379:        "Set stop_review_loop=true only when there are no blocking findings (P0/P1/P2, plus required_by_objective findings at any priority including P3), overall_correctness is patch is correct, goal_oracle_satisfied is true, and no objective-relevant implementation or validation remains.",
packages/workflows/builtin/goal-prompts.ts:380:        "Do not hold stop_review_loop at false for consistent_with_objective P3 nice-to-haves, beyond_objective/contradicts_objective observations, the reviewer-quorum process itself, or an authorized post-approval final action such as PR/MR/review creation.",
packages/workflows/builtin/goal-reducer.ts:110:        reason: `Reviewer quorum met: ${completeVotes}/${options.reviewQuorum} reviewers independently reported stop_review_loop=true with no reviewer execution errors.`,
packages/workflows/builtin/goal-reducer.ts:98:  // solely from the reviewer's self-reported `stop_review_loop` flag (plus the
packages/workflows/builtin/goal-review.ts:117:    stopReviewLoop: args.decision.stop_review_loop,
packages/workflows/builtin/goal-review.ts:33: * The reviewer's self-reported `stop_review_loop` boolean is the single
packages/workflows/builtin/goal-review.ts:42: * `stop_review_loop: false` decision, so parse failures never approve either.
packages/workflows/builtin/goal-review.ts:45:  return decision.stop_review_loop === true && decision.reviewer_error == null;
packages/workflows/builtin/goal-review.ts:60:    stop_review_loop: false,
packages/workflows/builtin/goal-schemas.ts:76:    stop_review_loop: Type.Boolean(),
packages/workflows/builtin/goal-types.ts:65:  readonly stop_review_loop: boolean;
packages/workflows/builtin/ralph-core.ts:104:    stop_review_loop: Type.Boolean(),
packages/workflows/builtin/ralph-core.ts:242:    stopReviewLoop: args.decision.stop_review_loop,
packages/workflows/builtin/ralph-core.ts:256:    stop_review_loop: false,
packages/workflows/builtin/ralph-review-gate.ts:101:  return decision.stop_review_loop === true && decision.reviewer_error == null;
packages/workflows/builtin/ralph-review-gate.ts:25: * `stop_review_loop: false` decision, so parse failures never approve either.
packages/workflows/builtin/ralph-review-gate.ts:6: * The reviewer's self-reported `stop_review_loop` boolean is the single
packages/workflows/builtin/ralph-review-gate.ts:71:  readonly stop_review_loop: boolean;
packages/workflows/builtin/ralph-review-gate.ts:90: * longer overrides the reviewer's `stop_review_loop` boolean.
packages/workflows/builtin/ralph-review-gate.ts:98: * when it set `stop_review_loop` to `true` and reported no execution error.
packages/workflows/builtin/ralph-reviewer-prompt.ts:106:        "If no finding clears this bar, return an empty findings array, mark the patch correct, and set stop_review_loop true. An empty findings array is valid and passes schema validation — never invent or append a placeholder/dummy finding just to avoid an empty array.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:149:        "8. Refuse approval when any material literal clause remains unverified: use the existing traceability, finding, and reviewer_error fields as applicable and set stop_review_loop=false.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:150:        "9. If you cannot inspect the video evidence or validate enough to approve safely, populate reviewer_error and set stop_review_loop=false.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:166:        "Always return requirements_traceability as a non-empty array that enumerates every explicit prompt and acceptance_criteria clause. Traceability and findings are audit evidence for humans and later stages; the harness gates approval on your stop_review_loop boolean alone, so derive that flag from them carefully.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:167:        "When setting stop_review_loop=true, every implementation/validation requirements_traceability entry must be proven, overall_correctness must be patch is correct, and reviewer_error must be null or omitted.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:168:        "Clauses that only the workflow process can satisfy — reviewer quorum/approval-count clauses, and (when create_pr is enabled) the post-approval PR/MR/review creation final action — are never implementation gaps: record them as final-action/process items and do not let them hold stop_review_loop at false.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:174:        "stop_review_loop is the single authoritative convergence flag: the harness approves this review exactly when stop_review_loop=true and reviewer_error is null/omitted, without recomputing approval from findings or traceability.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:175:        "Set stop_review_loop=true only when the patch is correct, reviewer_error is null/omitted, there are no blocking objective-aligned findings (P0/P1/P2, plus required_by_objective findings at any priority including P3), and no objective-relevant implementation or validation remains; beyond_objective and contradicts_objective findings are non-blocking and must not be folded into follow-up objectives without checking the literal contract.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:176:        "Do not hold stop_review_loop at false for consistent_with_objective P3 nice-to-haves, beyond_objective/contradicts_objective observations, the reviewer-quorum process itself, or an authorized post-approval final action such as PR/MR/review creation.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:178:        "If you hit a reviewer/tool/validation error, set stop_review_loop=false and populate reviewer_error instead of pretending the patch is approved.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:78:            "If the repository state satisfies every implementation and validation requirement and only PR/MR/review creation remains, approve the implementation: set overall_correctness to patch is correct, stop_review_loop=true, no blocking findings, and note the PR as the remaining final action rather than an implementation gap.",
packages/workflows/builtin/shared-prompts.ts:102:  "- A compile, type, build, or schema requirement without its applicable independent probe remains unverified: keep its requirements_traceability status missing, explain the gap, add an objective-aligned finding when the patch is materially deficient, and set stop_review_loop=false.",
packages/workflows/builtin/shared-prompts.ts:103:  "- When an applicable material probe is missing, blocked, or failed, record the command or scenario and its observed result or limitation in overall_explanation and requirements_traceability, use the workflow's existing remaining-verification or finding fields, and set stop_review_loop=false. When tools or dependencies prevent necessary verification after reasonable recovery, populate the existing reviewer_error field instead of approving around the limitation.",
packages/workflows/builtin/shared-prompts.ts:106:  "- Before returning stop_review_loop=true, confirm overall_correctness is patch is correct; every objective-relevant implementation and validation requirements_traceability entry is proven; no blocking objective-aligned finding remains; every applicable exact API, build, schema, state, configuration, and feature-flag risk has direct evidence or a clear explanation of why it does not apply; and reviewer_error is null or omitted.",
packages/workflows/builtin/shared-prompts.ts:107:  "- If any item in this self-audit is false or unverified, set stop_review_loop=false and report the gap through the existing fields; never make the structured verdict internally inconsistent.",
packages/workflows/builtin/shared-prompts.ts:125:  "Convergence flag (stop_review_loop):",
packages/workflows/builtin/shared-prompts.ts:126:  "- The reviewer's stop_review_loop boolean is the single authoritative convergence signal. The harness gates approval on that flag deterministically and does not recompute approval from findings arrays, priorities, or requirements_traceability statuses — derive the flag carefully because it is trusted as-is.",
packages/workflows/builtin/shared-prompts.ts:127:  "- Derive stop_review_loop=false while any objective-relevant blocking work remains: any P0/P1/P2 finding, any required_by_objective finding at any priority (P3 included — severity labels alone never dismiss objective-relevant findings), or any unproven implementation/validation requirement.",
packages/workflows/builtin/shared-prompts.ts:128:  "- Derive stop_review_loop=true when independent verification proves the implementation and validation requirements and everything left is non-blocking: consistent_with_objective P3 nice-to-haves, beyond_objective/contradicts_objective observations, an explicitly authorized post-approval final action such as PR/MR/review creation, or the multi-reviewer quorum process itself. Never hold the flag at false for those items — quorum is counted by the harness across reviewers and is not an implementation gap any single reviewer can prove.",
packages/workflows/builtin/shared-prompts.ts:143:  "- Never set stop_review_loop=true for an implementation objective when the review checkout's delta is empty or unrelated to that objective; an empty delta cannot satisfy an implementation objective regardless of what receipts claim.",

## objective_alignment
packages/workflows/builtin/goal-prompts.ts:360:        "Every finding must include objective_alignment: required_by_objective (the objective/acceptance criteria require fixing it), consistent_with_objective (valid defect within scope), beyond_objective (real issue but not required by objective/acceptance criteria and must not block completion or become a follow-up requirement without explicit reconciliation), or contradicts_objective (fixing it would violate literal wording and must never be implemented; escalate to the human).",
packages/workflows/builtin/goal-reports.ts:18:              `- ${finding.objective_alignment}: ${finding.title}`
packages/workflows/builtin/goal-review.ts:102:      `[${finding.objective_alignment}] ${finding.title}: ${finding.body}`
packages/workflows/builtin/goal-schemas.ts:8:    objective_alignment: Type.Union([
packages/workflows/builtin/goal-types.ts:35:  readonly objective_alignment: ObjectiveAlignment;
packages/workflows/builtin/ralph-core.ts:40:    objective_alignment: Type.Union([
packages/workflows/builtin/ralph-review-gate.ts:38:  readonly objective_alignment: ObjectiveAlignment;
packages/workflows/builtin/ralph-reviewer-prompt.ts:113:        "Also include numeric priority: 0 for P0, 1 for P1, 2 for P2, 3 for P3; use null only if priority genuinely cannot be determined. Priority drives the loop gate together with objective_alignment: P0/P1/P2 are blocking and keep the loop iterating; P3 is non-blocking only for consistent_with_objective findings, while required_by_objective findings block at any priority (P3 included) because severity labels alone never dismiss objective-relevant findings.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:114:        "Classify every finding with objective_alignment: required_by_objective (the objective/acceptance criteria require fixing it), consistent_with_objective (valid defect within scope), beyond_objective (real issue but not required and must not block or be promoted without explicit reconciliation), or contradicts_objective (fixing it would violate literal objective wording and must never be implemented; escalate to the human). Missing/unknown classification is blocking.",
packages/workflows/builtin/review-convergence.ts:72:  readonly objective_alignment?: string;
packages/workflows/builtin/review-convergence.ts:92:  const alignment = finding.objective_alignment;

## required_by_objective
packages/workflows/builtin/goal-prompts.ts:360:        "Every finding must include objective_alignment: required_by_objective (the objective/acceptance criteria require fixing it), consistent_with_objective (valid defect within scope), beyond_objective (real issue but not required by objective/acceptance criteria and must not block completion or become a follow-up requirement without explicit reconciliation), or contradicts_objective (fixing it would violate literal wording and must never be implemented; escalate to the human).",
packages/workflows/builtin/goal-prompts.ts:379:        "Set stop_review_loop=true only when there are no blocking findings (P0/P1/P2, plus required_by_objective findings at any priority including P3), overall_correctness is patch is correct, goal_oracle_satisfied is true, and no objective-relevant implementation or validation remains.",
packages/workflows/builtin/goal-prompts.ts:382:        "P3 findings are non-blocking only when classified consistent_with_objective; findings classified required_by_objective block at any priority (P3 included) because severity labels alone never dismiss objective-relevant findings. Do not use P3 for work required by the objective or verification oracle. Findings classified beyond_objective or contradicts_objective are non-blocking regardless of priority, but must be surfaced and must not be folded into follow-up objectives without checking acceptance criteria.",
packages/workflows/builtin/goal-schemas.ts:9:      Type.Literal("required_by_objective"),
packages/workflows/builtin/goal-types.ts:20:  | "required_by_objective"
packages/workflows/builtin/ralph-core.ts:41:      Type.Literal("required_by_objective"),
packages/workflows/builtin/ralph-review-gate.ts:12: * and required_by_objective findings at any priority mean `false`; in-scope
packages/workflows/builtin/ralph-review-gate.ts:29:  | "required_by_objective"
packages/workflows/builtin/ralph-review-gate.ts:78: * `required_by_objective` findings block regardless of priority.
packages/workflows/builtin/ralph-reviewer-prompt.ts:113:        "Also include numeric priority: 0 for P0, 1 for P1, 2 for P2, 3 for P3; use null only if priority genuinely cannot be determined. Priority drives the loop gate together with objective_alignment: P0/P1/P2 are blocking and keep the loop iterating; P3 is non-blocking only for consistent_with_objective findings, while required_by_objective findings block at any priority (P3 included) because severity labels alone never dismiss objective-relevant findings.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:114:        "Classify every finding with objective_alignment: required_by_objective (the objective/acceptance criteria require fixing it), consistent_with_objective (valid defect within scope), beyond_objective (real issue but not required and must not block or be promoted without explicit reconciliation), or contradicts_objective (fixing it would violate literal objective wording and must never be implemented; escalate to the human). Missing/unknown classification is blocking.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:175:        "Set stop_review_loop=true only when the patch is correct, reviewer_error is null/omitted, there are no blocking objective-aligned findings (P0/P1/P2, plus required_by_objective findings at any priority including P3), and no objective-relevant implementation or validation remains; beyond_objective and contradicts_objective findings are non-blocking and must not be folded into follow-up objectives without checking the literal contract.",
packages/workflows/builtin/review-convergence.ts:81: * - `required_by_objective` findings block at ANY priority — severity labels
packages/workflows/builtin/review-convergence.ts:96:  if (alignment === "required_by_objective") return true;
packages/workflows/builtin/shared-prompts.ts:127:  "- Derive stop_review_loop=false while any objective-relevant blocking work remains: any P0/P1/P2 finding, any required_by_objective finding at any priority (P3 included — severity labels alone never dismiss objective-relevant findings), or any unproven implementation/validation requirement.",
packages/workflows/builtin/shared-prompts.ts:142:  "- Use the repository's version-control tooling to inspect state (for git: `git worktree list`, `git status --short`, and a diff against the baseline branch; use the equivalent commands for other systems). If receipts, implementation notes, or stage summaries claim implemented work but the review checkout shows no corresponding delta, that is a blocking [P0] required_by_objective finding: the work may be stranded in another worktree, clone, or unapplied state. Do not approve; require the work to be brought into the review checkout first.",
packages/workflows/builtin/shared-prompts.ts:58:  "Hunt over-implementation as seriously as gaps: any validation error, required field, uniqueness/format constraint, immutability wrapper, or normalization the contract does not require is a defect that rejects inputs or produces shapes the contract permits — classify it required_by_objective. Probe at least one contract-permitted input the implementation's own tests do not exercise before approving.";

## consistent_with_objective
packages/workflows/builtin/goal-prompts.ts:360:        "Every finding must include objective_alignment: required_by_objective (the objective/acceptance criteria require fixing it), consistent_with_objective (valid defect within scope), beyond_objective (real issue but not required by objective/acceptance criteria and must not block completion or become a follow-up requirement without explicit reconciliation), or contradicts_objective (fixing it would violate literal wording and must never be implemented; escalate to the human).",
packages/workflows/builtin/goal-prompts.ts:380:        "Do not hold stop_review_loop at false for consistent_with_objective P3 nice-to-haves, beyond_objective/contradicts_objective observations, the reviewer-quorum process itself, or an authorized post-approval final action such as PR/MR/review creation.",
packages/workflows/builtin/goal-prompts.ts:382:        "P3 findings are non-blocking only when classified consistent_with_objective; findings classified required_by_objective block at any priority (P3 included) because severity labels alone never dismiss objective-relevant findings. Do not use P3 for work required by the objective or verification oracle. Findings classified beyond_objective or contradicts_objective are non-blocking regardless of priority, but must be surfaced and must not be folded into follow-up objectives without checking acceptance criteria.",
packages/workflows/builtin/goal-schemas.ts:10:      Type.Literal("consistent_with_objective"),
packages/workflows/builtin/goal-types.ts:21:  | "consistent_with_objective"
packages/workflows/builtin/ralph-core.ts:42:      Type.Literal("consistent_with_objective"),
packages/workflows/builtin/ralph-review-gate.ts:30:  | "consistent_with_objective"
packages/workflows/builtin/ralph-review-gate.ts:77: * `consistent_with_objective` findings. P0=0, P1=1, P2=2 block; P3=3 does not.
packages/workflows/builtin/ralph-reviewer-prompt.ts:113:        "Also include numeric priority: 0 for P0, 1 for P1, 2 for P2, 3 for P3; use null only if priority genuinely cannot be determined. Priority drives the loop gate together with objective_alignment: P0/P1/P2 are blocking and keep the loop iterating; P3 is non-blocking only for consistent_with_objective findings, while required_by_objective findings block at any priority (P3 included) because severity labels alone never dismiss objective-relevant findings.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:114:        "Classify every finding with objective_alignment: required_by_objective (the objective/acceptance criteria require fixing it), consistent_with_objective (valid defect within scope), beyond_objective (real issue but not required and must not block or be promoted without explicit reconciliation), or contradicts_objective (fixing it would violate literal objective wording and must never be implemented; escalate to the human). Missing/unknown classification is blocking.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:176:        "Do not hold stop_review_loop at false for consistent_with_objective P3 nice-to-haves, beyond_objective/contradicts_objective observations, the reviewer-quorum process itself, or an authorized post-approval final action such as PR/MR/review creation.",
packages/workflows/builtin/review-convergence.ts:65: * in-scope (`consistent_with_objective`) findings. P0=0, P1=1, P2=2 block;
packages/workflows/builtin/review-convergence.ts:83: * - `consistent_with_objective` findings block at P0/P1/P2; P3 is a
packages/workflows/builtin/review-convergence.ts:97:  if (alignment !== "consistent_with_objective") return true;
packages/workflows/builtin/shared-prompts.ts:128:  "- Derive stop_review_loop=true when independent verification proves the implementation and validation requirements and everything left is non-blocking: consistent_with_objective P3 nice-to-haves, beyond_objective/contradicts_objective observations, an explicitly authorized post-approval final action such as PR/MR/review creation, or the multi-reviewer quorum process itself. Never hold the flag at false for those items — quorum is counted by the harness across reviewers and is not an implementation gap any single reviewer can prove.",

## beyond_objective
packages/workflows/builtin/goal-prompts.ts:360:        "Every finding must include objective_alignment: required_by_objective (the objective/acceptance criteria require fixing it), consistent_with_objective (valid defect within scope), beyond_objective (real issue but not required by objective/acceptance criteria and must not block completion or become a follow-up requirement without explicit reconciliation), or contradicts_objective (fixing it would violate literal wording and must never be implemented; escalate to the human).",
packages/workflows/builtin/goal-prompts.ts:380:        "Do not hold stop_review_loop at false for consistent_with_objective P3 nice-to-haves, beyond_objective/contradicts_objective observations, the reviewer-quorum process itself, or an authorized post-approval final action such as PR/MR/review creation.",
packages/workflows/builtin/goal-prompts.ts:382:        "P3 findings are non-blocking only when classified consistent_with_objective; findings classified required_by_objective block at any priority (P3 included) because severity labels alone never dismiss objective-relevant findings. Do not use P3 for work required by the objective or verification oracle. Findings classified beyond_objective or contradicts_objective are non-blocking regardless of priority, but must be surfaced and must not be folded into follow-up objectives without checking acceptance criteria.",
packages/workflows/builtin/goal-reports.ts:12:      "Finding alignment warning: beyond_objective and contradicts_objective findings are non-blocking and must not be folded into follow-up objectives without checking them against the acceptance criteria.",
packages/workflows/builtin/goal-reports.ts:71:    "Review findings classified beyond_objective or contradicts_objective are non-blocking and must not be promoted into follow-up objectives without checking them against the acceptance criteria.",
packages/workflows/builtin/goal-schemas.ts:11:      Type.Literal("beyond_objective"),
packages/workflows/builtin/goal-types.ts:22:  | "beyond_objective"
packages/workflows/builtin/ralph-core.ts:43:      Type.Literal("beyond_objective"),
packages/workflows/builtin/ralph-review-gate.ts:31:  | "beyond_objective"
packages/workflows/builtin/ralph-reviewer-prompt.ts:114:        "Classify every finding with objective_alignment: required_by_objective (the objective/acceptance criteria require fixing it), consistent_with_objective (valid defect within scope), beyond_objective (real issue but not required and must not block or be promoted without explicit reconciliation), or contradicts_objective (fixing it would violate literal objective wording and must never be implemented; escalate to the human). Missing/unknown classification is blocking.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:175:        "Set stop_review_loop=true only when the patch is correct, reviewer_error is null/omitted, there are no blocking objective-aligned findings (P0/P1/P2, plus required_by_objective findings at any priority including P3), and no objective-relevant implementation or validation remains; beyond_objective and contradicts_objective findings are non-blocking and must not be folded into follow-up objectives without checking the literal contract.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:176:        "Do not hold stop_review_loop at false for consistent_with_objective P3 nice-to-haves, beyond_objective/contradicts_objective observations, the reviewer-quorum process itself, or an authorized post-approval final action such as PR/MR/review creation.",
packages/workflows/builtin/review-convergence.ts:86: * - `beyond_objective` / `contradicts_objective` findings never block: the
packages/workflows/builtin/review-convergence.ts:93:  if (alignment === "beyond_objective" || alignment === "contradicts_objective") {
packages/workflows/builtin/shared-prompts.ts:128:  "- Derive stop_review_loop=true when independent verification proves the implementation and validation requirements and everything left is non-blocking: consistent_with_objective P3 nice-to-haves, beyond_objective/contradicts_objective observations, an explicitly authorized post-approval final action such as PR/MR/review creation, or the multi-reviewer quorum process itself. Never hold the flag at false for those items — quorum is counted by the harness across reviewers and is not an implementation gap any single reviewer can prove.",
packages/workflows/builtin/shared-prompts.ts:55:  "Do not use external spec/standard conformance alone to flag a wide trigger surface for an error condition the objective/acceptance criteria enumerate; the contract prefers loud errors over silent reinterpretation of ambiguous inputs, so classify such spec-vs-objective tension as beyond_objective rather than a blocking defect.";

## contradicts_objective
packages/workflows/builtin/goal-prompts.ts:360:        "Every finding must include objective_alignment: required_by_objective (the objective/acceptance criteria require fixing it), consistent_with_objective (valid defect within scope), beyond_objective (real issue but not required by objective/acceptance criteria and must not block completion or become a follow-up requirement without explicit reconciliation), or contradicts_objective (fixing it would violate literal wording and must never be implemented; escalate to the human).",
packages/workflows/builtin/goal-prompts.ts:380:        "Do not hold stop_review_loop at false for consistent_with_objective P3 nice-to-haves, beyond_objective/contradicts_objective observations, the reviewer-quorum process itself, or an authorized post-approval final action such as PR/MR/review creation.",
packages/workflows/builtin/goal-prompts.ts:382:        "P3 findings are non-blocking only when classified consistent_with_objective; findings classified required_by_objective block at any priority (P3 included) because severity labels alone never dismiss objective-relevant findings. Do not use P3 for work required by the objective or verification oracle. Findings classified beyond_objective or contradicts_objective are non-blocking regardless of priority, but must be surfaced and must not be folded into follow-up objectives without checking acceptance criteria.",
packages/workflows/builtin/goal-reports.ts:12:      "Finding alignment warning: beyond_objective and contradicts_objective findings are non-blocking and must not be folded into follow-up objectives without checking them against the acceptance criteria.",
packages/workflows/builtin/goal-reports.ts:71:    "Review findings classified beyond_objective or contradicts_objective are non-blocking and must not be promoted into follow-up objectives without checking them against the acceptance criteria.",
packages/workflows/builtin/goal-schemas.ts:12:      Type.Literal("contradicts_objective"),
packages/workflows/builtin/goal-types.ts:23:  | "contradicts_objective";
packages/workflows/builtin/ralph-core.ts:44:      Type.Literal("contradicts_objective"),
packages/workflows/builtin/ralph-review-gate.ts:32:  | "contradicts_objective";
packages/workflows/builtin/ralph-reviewer-prompt.ts:114:        "Classify every finding with objective_alignment: required_by_objective (the objective/acceptance criteria require fixing it), consistent_with_objective (valid defect within scope), beyond_objective (real issue but not required and must not block or be promoted without explicit reconciliation), or contradicts_objective (fixing it would violate literal objective wording and must never be implemented; escalate to the human). Missing/unknown classification is blocking.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:175:        "Set stop_review_loop=true only when the patch is correct, reviewer_error is null/omitted, there are no blocking objective-aligned findings (P0/P1/P2, plus required_by_objective findings at any priority including P3), and no objective-relevant implementation or validation remains; beyond_objective and contradicts_objective findings are non-blocking and must not be folded into follow-up objectives without checking the literal contract.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:176:        "Do not hold stop_review_loop at false for consistent_with_objective P3 nice-to-haves, beyond_objective/contradicts_objective observations, the reviewer-quorum process itself, or an authorized post-approval final action such as PR/MR/review creation.",
packages/workflows/builtin/review-convergence.ts:86: * - `beyond_objective` / `contradicts_objective` findings never block: the
packages/workflows/builtin/review-convergence.ts:93:  if (alignment === "beyond_objective" || alignment === "contradicts_objective") {
packages/workflows/builtin/shared-prompts.ts:128:  "- Derive stop_review_loop=true when independent verification proves the implementation and validation requirements and everything left is non-blocking: consistent_with_objective P3 nice-to-haves, beyond_objective/contradicts_objective observations, an explicitly authorized post-approval final action such as PR/MR/review creation, or the multi-reviewer quorum process itself. Never hold the flag at false for those items — quorum is counted by the harness across reviewers and is not an implementation gap any single reviewer can prove.",

## requirements_traceability
packages/workflows/builtin/goal-prompts.ts:355:        "Record every applicable independent probe's command or scenario and observed result in overall_explanation, receipt_assessment, verification_remaining, and requirements_traceability; do not cite a passing implementation-authored test alone for an exact API, build, or schema clause.",
packages/workflows/builtin/goal-prompts.ts:368:        "Always return requirements_traceability as a non-empty array that enumerates every explicit objective and acceptance-criteria clause. Traceability and findings are audit evidence for humans and later stages; the harness gates approval on your stop_review_loop boolean alone, so derive that flag from them carefully.",
packages/workflows/builtin/goal-prompts.ts:369:        "When setting stop_review_loop=true, every implementation/validation requirements_traceability entry must be proven, goal_oracle_satisfied must be true, verification_remaining must say no objective-relevant implementation or validation remains, and reviewer_error must be null or omitted.",
packages/workflows/builtin/goal-prompts.ts:381:        "Enumerate every explicit requirement clause from the objective and acceptance criteria in requirements_traceability, including clauses about existing tests/snapshots and expected behavior. Treat implementation-authored tests or snapshots passing as circular evidence that cannot by itself prove a clause.",
packages/workflows/builtin/goal-reports.ts:21:      review.requirements_traceability.length === 0
packages/workflows/builtin/goal-reports.ts:25:            ...review.requirements_traceability.map((entry) =>
packages/workflows/builtin/goal-review.ts:35: * from findings arrays, priorities, or requirements_traceability statuses.
packages/workflows/builtin/goal-review.ts:56:    requirements_traceability: [],
packages/workflows/builtin/goal-review.ts:95:    finalActionRemaining(args.decision.requirements_traceability);
packages/workflows/builtin/goal-review.ts:97:  const traceabilityGaps = args.decision.requirements_traceability
packages/workflows/builtin/goal-schemas.ts:73:    requirements_traceability: Type.Array(requirementsTraceabilitySchema),
packages/workflows/builtin/goal-types.ts:62:  readonly requirements_traceability: readonly RequirementTraceability[];
packages/workflows/builtin/ralph-core.ts:103:    requirements_traceability: Type.Array(requirementsTraceabilitySchema),
packages/workflows/builtin/ralph-core.ts:238:    finalActionRemaining(args.decision.requirements_traceability);
packages/workflows/builtin/ralph-core.ts:257:    requirements_traceability: [],
packages/workflows/builtin/ralph-review-gate.ts:70:  readonly requirements_traceability: readonly RequirementTraceability[];
packages/workflows/builtin/ralph-review-gate.ts:9: * requirements_traceability statuses: those fields remain required audit
packages/workflows/builtin/ralph-reviewer-prompt.ts:146:        "5. Name each independent probe executed and its outcome in overall_explanation and the corresponding requirements_traceability evidence.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:157:        "Each requirements_traceability evidence entry must distinguish direct independent proof from worker-authored or repository-local test corroboration.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:166:        "Always return requirements_traceability as a non-empty array that enumerates every explicit prompt and acceptance_criteria clause. Traceability and findings are audit evidence for humans and later stages; the harness gates approval on your stop_review_loop boolean alone, so derive that flag from them carefully.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:167:        "When setting stop_review_loop=true, every implementation/validation requirements_traceability entry must be proven, overall_correctness must be patch is correct, and reviewer_error must be null or omitted.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:177:        "Enumerate every explicit requirement clause from the prompt and acceptance_criteria in requirements_traceability, including clauses about existing tests/snapshots and expected behavior. Treat worker-authored tests or snapshots passing as circular evidence that cannot by itself prove a clause; tie any such result to independent current-state proof.",
packages/workflows/builtin/shared-prompts.ts:100:  "- Execute or delegate every applicable material probe against the current repository state before mapping implementation evidence to requirements. Name each command or scenario and its observed result in the existing narrative and requirements_traceability fields.",
packages/workflows/builtin/shared-prompts.ts:102:  "- A compile, type, build, or schema requirement without its applicable independent probe remains unverified: keep its requirements_traceability status missing, explain the gap, add an objective-aligned finding when the patch is materially deficient, and set stop_review_loop=false.",
packages/workflows/builtin/shared-prompts.ts:103:  "- When an applicable material probe is missing, blocked, or failed, record the command or scenario and its observed result or limitation in overall_explanation and requirements_traceability, use the workflow's existing remaining-verification or finding fields, and set stop_review_loop=false. When tools or dependencies prevent necessary verification after reasonable recovery, populate the existing reviewer_error field instead of approving around the limitation.",
packages/workflows/builtin/shared-prompts.ts:106:  "- Before returning stop_review_loop=true, confirm overall_correctness is patch is correct; every objective-relevant implementation and validation requirements_traceability entry is proven; no blocking objective-aligned finding remains; every applicable exact API, build, schema, state, configuration, and feature-flag risk has direct evidence or a clear explanation of why it does not apply; and reviewer_error is null or omitted.",
packages/workflows/builtin/shared-prompts.ts:126:  "- The reviewer's stop_review_loop boolean is the single authoritative convergence signal. The harness gates approval on that flag deterministically and does not recompute approval from findings arrays, priorities, or requirements_traceability statuses — derive the flag carefully because it is trusted as-is.",

## overall_correctness
packages/workflows/builtin/goal-prompts.ts:379:        "Set stop_review_loop=true only when there are no blocking findings (P0/P1/P2, plus required_by_objective findings at any priority including P3), overall_correctness is patch is correct, goal_oracle_satisfied is true, and no objective-relevant implementation or validation remains.",
packages/workflows/builtin/goal-review.ts:51:    overall_correctness: "patch is incorrect",
packages/workflows/builtin/goal-schemas.ts:66:    overall_correctness: Type.Union([
packages/workflows/builtin/goal-types.ts:58:  readonly overall_correctness: "patch is correct" | "patch is incorrect";
packages/workflows/builtin/ralph-core.ts:252:    overall_correctness: "patch is incorrect",
packages/workflows/builtin/ralph-core.ts:97:    overall_correctness: Type.Union([
packages/workflows/builtin/ralph-review-gate.ts:67:  readonly overall_correctness: "patch is correct" | "patch is incorrect";
packages/workflows/builtin/ralph-reviewer-prompt.ts:167:        "When setting stop_review_loop=true, every implementation/validation requirements_traceability entry must be proven, overall_correctness must be patch is correct, and reviewer_error must be null or omitted.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:78:            "If the repository state satisfies every implementation and validation requirement and only PR/MR/review creation remains, approve the implementation: set overall_correctness to patch is correct, stop_review_loop=true, no blocking findings, and note the PR as the remaining final action rather than an implementation gap.",
packages/workflows/builtin/shared-prompts.ts:106:  "- Before returning stop_review_loop=true, confirm overall_correctness is patch is correct; every objective-relevant implementation and validation requirements_traceability entry is proven; no blocking objective-aligned finding remains; every applicable exact API, build, schema, state, configuration, and feature-flag risk has direct evidence or a clear explanation of why it does not apply; and reviewer_error is null or omitted.",

## reviewer_error
packages/workflows/builtin/goal-prompts.ts:278:        "If validation cannot be completed after reasonable recovery, record the limitation in overall_explanation and reviewer_error; do not use missing dependencies as a reason to approve.",
packages/workflows/builtin/goal-prompts.ts:340:        "8. If tools or dependencies prevent necessary verification after reasonable recovery, populate reviewer_error and set stop_review_loop=false rather than approving around the limitation.",
packages/workflows/builtin/goal-prompts.ts:347:        "If the strict blocked audit is satisfied by current evidence, do not invent a finding. Set stop_review_loop=false, goal_oracle_satisfied=false, verification_remaining to the concise blocker, and reviewer_error.kind to dependency_unavailable or tool_failure with reviewer_error.message set to the same concise blocker.",
packages/workflows/builtin/goal-prompts.ts:348:        "When the same dependency or tool blocker from prior reviewer history is still present, echo the prior blocker string in verification_remaining and reviewer_error.message instead of rephrasing it.",
packages/workflows/builtin/goal-prompts.ts:349:        "Use reviewer_error for a blocker only when there is a real impasse that prevents meaningful progress without user input or an external-state change; never for ordinary incomplete work, uncertainty, or useful work remaining.",
packages/workflows/builtin/goal-prompts.ts:369:        "When setting stop_review_loop=true, every implementation/validation requirements_traceability entry must be proven, goal_oracle_satisfied must be true, verification_remaining must say no objective-relevant implementation or validation remains, and reviewer_error must be null or omitted.",
packages/workflows/builtin/goal-prompts.ts:372:        "If you hit a reviewer/tool/validation error, set stop_review_loop=false and populate reviewer_error instead of pretending the patch is approved.",
packages/workflows/builtin/goal-prompts.ts:378:        "stop_review_loop is the single authoritative convergence flag: the harness approves this review exactly when stop_review_loop=true and reviewer_error is null/omitted, without recomputing approval from findings or traceability.",
packages/workflows/builtin/goal-reducer.ts:99:  // reviewer_error/parse-failure guards). The reducer completes on quorum of
packages/workflows/builtin/goal-review.ts:106:    ...(args.decision.reviewer_error == null
packages/workflows/builtin/goal-review.ts:108:      : [`${args.decision.reviewer_error.kind}: ${args.decision.reviewer_error.message}`]),
packages/workflows/builtin/goal-review.ts:40: * Two hard guards remain: a reviewer execution failure (`reviewer_error`)
packages/workflows/builtin/goal-review.ts:45:  return decision.stop_review_loop === true && decision.reviewer_error == null;
packages/workflows/builtin/goal-review.ts:61:    reviewer_error: {
packages/workflows/builtin/goal-review.ts:71:  const reviewerError = decision.reviewer_error;
packages/workflows/builtin/goal-schemas.ts:77:    reviewer_error: Type.Optional(
packages/workflows/builtin/goal-types.ts:66:  readonly reviewer_error?: ReviewerError | null;
packages/workflows/builtin/ralph-core.ts:105:    reviewer_error: Type.Optional(
packages/workflows/builtin/ralph-core.ts:258:    reviewer_error: {
packages/workflows/builtin/ralph-core.ts:27:// approves when it judged the patch correct, reported no reviewer_error, and
packages/workflows/builtin/ralph-review-gate.ts:101:  return decision.stop_review_loop === true && decision.reviewer_error == null;
packages/workflows/builtin/ralph-review-gate.ts:23: * Two hard guards remain: a reviewer execution failure (`reviewer_error`)
packages/workflows/builtin/ralph-review-gate.ts:72:  readonly reviewer_error?: ReviewerError | null;
packages/workflows/builtin/ralph-reviewer-prompt.ts:149:        "8. Refuse approval when any material literal clause remains unverified: use the existing traceability, finding, and reviewer_error fields as applicable and set stop_review_loop=false.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:150:        "9. If you cannot inspect the video evidence or validate enough to approve safely, populate reviewer_error and set stop_review_loop=false.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:167:        "When setting stop_review_loop=true, every implementation/validation requirements_traceability entry must be proven, overall_correctness must be patch is correct, and reviewer_error must be null or omitted.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:174:        "stop_review_loop is the single authoritative convergence flag: the harness approves this review exactly when stop_review_loop=true and reviewer_error is null/omitted, without recomputing approval from findings or traceability.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:175:        "Set stop_review_loop=true only when the patch is correct, reviewer_error is null/omitted, there are no blocking objective-aligned findings (P0/P1/P2, plus required_by_objective findings at any priority including P3), and no objective-relevant implementation or validation remains; beyond_objective and contradicts_objective findings are non-blocking and must not be folded into follow-up objectives without checking the literal contract.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:178:        "If you hit a reviewer/tool/validation error, set stop_review_loop=false and populate reviewer_error instead of pretending the patch is approved.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:88:        "If validation cannot be completed after reasonable recovery, record the limitation in overall_explanation and reviewer_error; do not use missing dependencies as a reason to approve.",
packages/workflows/builtin/shared-prompts.ts:103:  "- When an applicable material probe is missing, blocked, or failed, record the command or scenario and its observed result or limitation in overall_explanation and requirements_traceability, use the workflow's existing remaining-verification or finding fields, and set stop_review_loop=false. When tools or dependencies prevent necessary verification after reasonable recovery, populate the existing reviewer_error field instead of approving around the limitation.",
packages/workflows/builtin/shared-prompts.ts:106:  "- Before returning stop_review_loop=true, confirm overall_correctness is patch is correct; every objective-relevant implementation and validation requirements_traceability entry is proven; no blocking objective-aligned finding remains; every applicable exact API, build, schema, state, configuration, and feature-flag risk has direct evidence or a clear explanation of why it does not apply; and reviewer_error is null or omitted.",

## overall_explanation
packages/workflows/builtin/goal-prompts.ts:278:        "If validation cannot be completed after reasonable recovery, record the limitation in overall_explanation and reviewer_error; do not use missing dependencies as a reason to approve.",
packages/workflows/builtin/goal-prompts.ts:355:        "Record every applicable independent probe's command or scenario and observed result in overall_explanation, receipt_assessment, verification_remaining, and requirements_traceability; do not cite a passing implementation-authored test alone for an exact API, build, or schema clause.",
packages/workflows/builtin/goal-prompts.ts:356:        "The overall_explanation should briefly mention what was inspected and what validation was run or why validation was not completed.",
packages/workflows/builtin/goal-review.ts:126:    evidence: [args.decision.receipt_assessment, args.decision.overall_explanation],
packages/workflows/builtin/goal-review.ts:130:    explanation: args.decision.overall_explanation,
packages/workflows/builtin/goal-review.ts:52:    overall_explanation:
packages/workflows/builtin/goal-schemas.ts:70:    overall_explanation: Type.String(),
packages/workflows/builtin/goal-types.ts:59:  readonly overall_explanation: string;
packages/workflows/builtin/ralph-core.ts:101:    overall_explanation: Type.String(),
packages/workflows/builtin/ralph-core.ts:253:    overall_explanation:
packages/workflows/builtin/ralph-review-gate.ts:68:  readonly overall_explanation: string;
packages/workflows/builtin/ralph-reviewer-prompt.ts:146:        "5. Name each independent probe executed and its outcome in overall_explanation and the corresponding requirements_traceability evidence.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:156:        "The overall_explanation must name every applicable independent probe's command or scenario and its observed result, or explain why a risk class does not apply.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:88:        "If validation cannot be completed after reasonable recovery, record the limitation in overall_explanation and reviewer_error; do not use missing dependencies as a reason to approve.",
packages/workflows/builtin/shared-prompts.ts:103:  "- When an applicable material probe is missing, blocked, or failed, record the command or scenario and its observed result or limitation in overall_explanation and requirements_traceability, use the workflow's existing remaining-verification or finding fields, and set stop_review_loop=false. When tools or dependencies prevent necessary verification after reasonable recovery, populate the existing reviewer_error field instead of approving around the limitation.",

## code_location
packages/workflows/builtin/goal-prompts.ts:306:        "Keep code_location ranges as short as possible, ideally one line and never longer than 5-10 lines unless unavoidable.",
packages/workflows/builtin/goal-prompts.ts:307:        "The code_location must overlap the diff/change under review.",
packages/workflows/builtin/goal-schemas.ts:17:    code_location: Type.Object(
packages/workflows/builtin/goal-types.ts:37:  readonly code_location: {
packages/workflows/builtin/ralph-core.ts:49:    code_location: Type.Object(
packages/workflows/builtin/ralph-review-gate.ts:40:  readonly code_location: {
packages/workflows/builtin/ralph-reviewer-prompt.ts:117:        "Keep code_location ranges as short as possible, ideally one line and never longer than 5-10 lines unless unavoidable.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:118:        "The code_location must overlap the diff/change under review.",
packages/workflows/builtin/review-convergence.ts:105:  readonly code_location?: {
packages/workflows/builtin/review-convergence.ts:122:  return `${finding.code_location?.absolute_file_path ?? ""}::${normalizedTitle}`;

## findings
packages/subagents/agents/code-simplifier.md:125:3. **Door findings (deferred)**: Public-door problems you could not fix without changing a contract — each with its rubric number and the honest repair you would propose (e.g., "`processPayment(): bool` — rubric #2/#3: the `bool` collapses declined / network-failure / duplicate into one `false`; propose a named `Result`")
packages/subagents/agents/code-simplifier.md:53:- **Out of scope**: Do not add new features, change public APIs, alter behavior, or perform large architectural rewrites unless explicitly requested. Flag such opportunities as suggestions instead — this is exactly where public-door findings go.
packages/subagents/agents/code-simplifier.md:68:3. **Plan refinements**: List candidate refinements. Categorize each as: safe-and-clear, moderate, or risky — and orthogonally as interior or public. Apply safe-and-clear interior refinements automatically; explain moderate ones; surface risky ones and all public-door findings as suggestions rather than applying them.
packages/subagents/agents/codebase-locator.md:56:Structure your findings like this:
packages/subagents/agents/codebase-online-researcher.md:163:5. **Synthesize findings**.
packages/subagents/agents/codebase-online-researcher.md:238:Structure your findings as:
packages/subagents/agents/codebase-online-researcher.md:242:[Brief overview of key findings]
packages/subagents/agents/codebase-online-researcher.md:301:Remember: you are the user's expert guide to technical research. Lean on `fetch_content` first with the `/llms.txt` → `Accept: text/markdown` → `playwright-cli` fallback chain to efficiently pull authoritative content, clone open-source repos when implementation evidence is needed, store anything reusable under `research/web/`, and deliver comprehensive, up-to-date answers with exact citations and GitHub permalinks. Answer directly — skip preamble like "I'll help you with…" and go straight to findings.
packages/subagents/agents/codebase-pattern-finder.md:62:Structure your findings like this:
packages/subagents/agents/codebase-research-locator.md:19:2. **Categorize findings by type**
packages/subagents/agents/codebase-research-locator.md:90:Structure your findings like this:
packages/subagents/prompts/gather-context-and-clarify.md:16:Give each subagent a specific meta prompt. Ask them to return concise findings plus the remaining clarification questions that matter for implementation confidence. None of these specialists should edit files — they are read-only context gatherers.
packages/subagents/prompts/parallel-cleanup.md:21:Tell this scout to treat tool output and slop-scan-style findings as leads, not verdicts. It should return only concrete issues in the requested scope with evidence, severity, file/line references, and the smallest safe fix.
packages/subagents/prompts/parallel-cleanup.md:38:Both scouts are read-only. `codebase-analyzer` cannot edit; do not ask it to. Their response should be evidence-backed findings with file/line references and suggested fixes, not a context summary.
packages/subagents/prompts/parallel-cleanup.md:50:Without autofix mode, ask before applying fixes unless I already told you to address the cleanup feedback. When you ask, end with a compact numbered menu so I can respond with a number. Use wording suited to the findings, but include these choices when applicable:
packages/subagents/prompts/parallel-research.md:37:- Time-sensitive topics: have the online researcher prefer 2026/2025 sources and persist findings to `research/web/`.
packages/subagents/prompts/parallel-research.md:41:Ask each specialist to return concise findings with evidence:
packages/subagents/prompts/parallel-research.md:43:- file paths and line ranges for local findings;
packages/subagents/prompts/parallel-research.md:44:- source links for external findings;
packages/subagents/prompts/parallel-research.md:56:If findings disagree, call out the disagreement instead of smoothing it over.
packages/subagents/prompts/parallel-review.md:15:   Treat the diff as a suspect change. Reproduce the relevant behavior when possible, hypothesize how it could break, and report findings with evidence. The `debugger` agent can write fixes — for this pass, explicitly instruct it to inspect and report only, not edit.
packages/subagents/prompts/parallel-review.md:28:Give every specialist a specific task prompt naming its angle. Ask them to return concise, evidence-backed findings with file/line references and suggested fixes. The response should be review feedback, not a context summary. Specialists must not edit files in this pass, even when the agent type can — say so explicitly in the prompt.
packages/subagents/prompts/parallel-review.md:40:Without autofix mode, ask before applying fixes unless I already told you to address review feedback. When you ask, end with a compact numbered menu so I can respond with a number. Use wording suited to the findings, but include these choices when applicable:
packages/subagents/prompts/review-loop.md:35:After a fix writer returns, run another review round only when it made material changes or addressed non-trivial findings. Do not keep looping for optional polish, speculative improvements, or findings already deferred by the parent.
packages/workflows/builtin/adversarial-verification-prompts.ts:10:  return `<role>\nYou reduce independent verification reports into one deterministic next action.\n</role>\n\n<objective>\nDecide whether the candidate for ${task} is accepted, rejected, or needs repair.\n</objective>\n\n<artifacts>\nCandidate: ${candidatePath}\nVerifier reports: ${verifierPaths.join(", ")}\n</artifacts>\n\n<decision_rules>\nAccept only when all material rubric requirements have pass evidence. Request repair when findings are actionable and repair budget remains (${repairsCompleted}/${maxRepairs}). Reject when evidence proves the candidate cannot satisfy the task or the repair budget is exhausted. Preserve unresolved blockers verbatim.\n</decision_rules>\n\n<output_format>\nCall structured_output with decision (accept, reject, or repair), rationale, and remaining_work.\n</output_format>`;
packages/workflows/builtin/adversarial-verification-prompts.ts:14:  return `<role>\nYou repair a candidate using independent blocking findings.\n</role>\n\n<objective>\nRepair the candidate for: ${task}\n</objective>\n\n<artifacts>\nRead the current candidate at ${candidatePath} and reducer report at ${reviewPath}.\n</artifacts>\n\n<requirements>\nAddress every actionable blocker, rerun relevant validation, and retain valid prior work. Do not dismiss a finding without contrary evidence.\n</requirements>\n\n<output_format>\nA complete replacement candidate with repair summary, evidence, validation, and remaining risks.\n</output_format>`;
packages/workflows/builtin/adversarial-verification-prompts.ts:6:  return `<role>\nYou are an independent adversarial verifier. Find blockers; do not rewrite the candidate.\n</role>\n\n<objective>\nVerify the candidate against the task and rubric. Task: ${task}\n</objective>\n\n<artifacts>\nRead the complete candidate at ${candidatePath} and rubric at ${rubricPath}.\n</artifacts>\n\n<requirements>\nTest important claims where practical. A pass requires concrete evidence for every rubric item. Report precise blocking findings; absence of evidence is not evidence of correctness.\n</requirements>\n\n<output_format>\nCall structured_output with verdict (pass or fail), evidence, and blocking_findings.\n</output_format>`;
packages/workflows/builtin/adversarial-verification-runner.ts:11:  blocking_findings: Type.Array(Type.String()),
packages/workflows/builtin/adversarial-verification-runner.ts:40:  return isRecord(value) && (value.verdict === "pass" || value.verdict === "fail") && Array.isArray(value.evidence) && value.evidence.every((item) => typeof item === "string") && Array.isArray(value.blocking_findings) && value.blocking_findings.every((item) => typeof item === "string");
packages/workflows/builtin/adversarial-verification-runner.ts:79:      const remaining = validReports.flatMap((report) => report.blocking_findings);
packages/workflows/builtin/adversarial-verification.ts:21:    remaining_work: Type.Array(Type.String(), { description: "Unresolved blocking findings when not approved." }),
packages/workflows/builtin/fan-out-and-synthesize-prompts.ts:10:  return `<role>\nYou synthesize independent branch artifacts at a strict barrier.\n</role>\n\n<objective>\nProduce the final answer for: ${prompt}\n</objective>\n\n<artifact_contract>\nRead ${manifestPath} first, then read every branch artifact listed there. Do not omit a completed branch and do not assume access to branch conversations.\n</artifact_contract>\n\n<success_criteria>\nDeduplicate overlapping findings, explicitly resolve or preserve conflicting claims, retain material uncertainty, and cite branch labels and artifact paths for important conclusions. The answer must be traceable to evidence rather than majority vote.\n</success_criteria>\n\n<output_format>\nMarkdown with Executive synthesis, Consolidated findings, Conflicts and resolutions, Evidence index, and Remaining uncertainty headings.\n</output_format>`;
packages/workflows/builtin/fan-out-and-synthesize-prompts.ts:6:  return `<role>\nYou own one independent branch of a larger task.\n</role>\n\n<overall_task>\n${input.prompt}\n</overall_task>\n\n<branch>\n${input.label}: ${input.objective}\n</branch>\n\n<success_criteria>\nInvestigate only this branch, cite concrete files/sources/commands or other observable evidence, distinguish findings from uncertainty, and produce a standalone artifact that a synthesizer can audit.\n</success_criteria>\n\n<output_format>\nMarkdown with Scope, Findings, Evidence, Conflicts or uncertainty, and Recommendations headings.\n</output_format>`;
packages/workflows/builtin/goal-artifacts.ts:48:  // Deduplicated cross-reviewer findings batch so the next orchestrator turn can
packages/workflows/builtin/goal-artifacts.ts:49:  // plan and repair the round's findings together instead of one at a time.
packages/workflows/builtin/goal-artifacts.ts:53:      findings: review.findings,
packages/workflows/builtin/goal-artifacts.ts:58:    `${JSON.stringify({ reviews: visibleReviews, consolidated_findings: consolidatedFindings }, null, 2)}\n`,
packages/workflows/builtin/goal-orchestrator-prompts.ts:122:        "All previously established guidance still applies unchanged: the role, goal invariants, project preflight, orchestrator receipt contract, completion audit, blocked audit, literal objective contract, acceptance matrix, adversarial divergence audit, findings batch, regression evidence, evidence closure, worktree discipline, PR handoff policy, orchestration and subagent-tracking guidance, E2E verification guidance, and receipt output format.",
packages/workflows/builtin/goal-orchestrator-prompts.ts:95:          "Decompose the work into delegated subagent tasks based on the literal objective, acceptance criteria, current repository state, and consolidated reviewer findings.",
packages/workflows/builtin/goal-orchestrator-prompts.ts:96:          "Pass each subagent the relevant task, current working directory, constraints, files, validation expectations, and unresolved reviewer findings it owns.",
packages/workflows/builtin/goal-prompts.ts:147:    "When a review-round artifact with a consolidated_findings batch is listed, read it first and treat that batch as the set of findings to repair together this turn.",
packages/workflows/builtin/goal-prompts.ts:179:    ["findings_batch", FINDINGS_CONSOLIDATION_CONTRACT],
packages/workflows/builtin/goal-prompts.ts:237:            "If the repository state satisfies every implementation and validation requirement and only PR/MR/review creation remains, approve the implementation: set goal_oracle_satisfied=true, stop_review_loop=true, no blocking findings, and note the PR as the remaining final action rather than an implementation gap.",
packages/workflows/builtin/goal-prompts.ts:296:        "If no finding clears this bar and receipts prove the objective, return an empty findings array, mark the patch correct, set goal_oracle_satisfied true, and set stop_review_loop true.",
packages/workflows/builtin/goal-prompts.ts:313:      "how_many_findings",
packages/workflows/builtin/goal-prompts.ts:315:        "Return all findings the original author would definitely want to fix.",
packages/workflows/builtin/goal-prompts.ts:316:        "If no such findings exist, return an empty findings array and mark the patch correct only when receipt-backed evidence also satisfies the full objective.",
packages/workflows/builtin/goal-prompts.ts:338:        "6. Run or delegate focused validation when needed to resolve uncertainty, and check that fixes for previously reproduced findings carry durable regression evidence.",
packages/workflows/builtin/goal-prompts.ts:367:        "Always return findings as an array; use [] when there are no findings and never invent placeholder findings.",
packages/workflows/builtin/goal-prompts.ts:368:        "Always return requirements_traceability as a non-empty array that enumerates every explicit objective and acceptance-criteria clause. Traceability and findings are audit evidence for humans and later stages; the harness gates approval on your stop_review_loop boolean alone, so derive that flag from them carefully.",
packages/workflows/builtin/goal-prompts.ts:370:        "Goal-specific pre-verdict self-audit: before stop_review_loop=true, confirm goal_oracle_satisfied is true and verification_remaining reports no objective-relevant verification gap, in addition to the correctness, traceability, findings, applicable-risk evidence, and reviewer-error checks in independent_verification.",
packages/workflows/builtin/goal-prompts.ts:378:        "stop_review_loop is the single authoritative convergence flag: the harness approves this review exactly when stop_review_loop=true and reviewer_error is null/omitted, without recomputing approval from findings or traceability.",
packages/workflows/builtin/goal-prompts.ts:379:        "Set stop_review_loop=true only when there are no blocking findings (P0/P1/P2, plus required_by_objective findings at any priority including P3), overall_correctness is patch is correct, goal_oracle_satisfied is true, and no objective-relevant implementation or validation remains.",
packages/workflows/builtin/goal-prompts.ts:382:        "P3 findings are non-blocking only when classified consistent_with_objective; findings classified required_by_objective block at any priority (P3 included) because severity labels alone never dismiss objective-relevant findings. Do not use P3 for work required by the objective or verification oracle. Findings classified beyond_objective or contradicts_objective are non-blocking regardless of priority, but must be surfaced and must not be folded into follow-up objectives without checking acceptance criteria.",
packages/workflows/builtin/goal-reducer.ts:100:  // those booleans and does not re-litigate findings arrays or traceability
packages/workflows/builtin/goal-reports.ts:12:      "Finding alignment warning: beyond_objective and contradicts_objective findings are non-blocking and must not be folded into follow-up objectives without checking them against the acceptance criteria.",
packages/workflows/builtin/goal-reports.ts:13:      review.findings.length === 0
packages/workflows/builtin/goal-reports.ts:17:            ...review.findings.map((finding) =>
packages/workflows/builtin/goal-reports.ts:71:    "Review findings classified beyond_objective or contradicts_objective are non-blocking and must not be promoted into follow-up objectives without checking them against the acceptance criteria.",
packages/workflows/builtin/goal-review.ts:101:    ...args.decision.findings.map((finding) =>
packages/workflows/builtin/goal-review.ts:35: * from findings arrays, priorities, or requirements_traceability statuses.
packages/workflows/builtin/goal-review.ts:50:    findings: [],
packages/workflows/builtin/goal-runner.ts:272:      // Consolidated round artifact leads so the next orchestrator turn plans the full findings batch first.
packages/workflows/builtin/goal-schemas.ts:65:    findings: Type.Array(reviewFindingSchema),
packages/workflows/builtin/goal-types.ts:57:  readonly findings: readonly ReviewFinding[];
packages/workflows/builtin/goal.ts:16:  description: "Goal Runner workflow with bounded sub-agent orchestration turns, immutable acceptance criteria, ledger artifacts, parallel reviewers, and reducer-gated completion. When launching follow-up goal runs from review findings, pass the ORIGINAL task text as acceptance_criteria so deltas cannot drift from the literal contract. If the task includes submitting a pull request (or MR/review), remove that final action from the objective text and set create_pr=true instead when preparing the workflow inputs.",
packages/workflows/builtin/goal.ts:19:    acceptance_criteria: Type.Optional(Type.String({ description: "Original immutable task contract this run must remain consistent with. Defaults to objective. Orchestrators launching follow-up runs from reviewer findings should pass the ORIGINAL task text here." })),
packages/workflows/builtin/loop-until-done-prompts.ts:19:    ["progress_ledger", `Read ${options.ledgerPath} first. It is the durable source of truth for attempted work, findings, failures, validation evidence, and remaining work.`],
packages/workflows/builtin/loop-until-done-prompts.ts:28:    ["output_format", "Markdown with Work performed, Changes, Validation evidence, New findings, Failures, and Remaining work."],
packages/workflows/builtin/loop-until-done-prompts.ts:53:      "Record new findings and failures distinctly.",
packages/workflows/builtin/loop-until-done-runner.ts:141:      findings: decision.newFindings,
packages/workflows/builtin/loop-until-done-runner.ts:20:  new_findings: Type.Array(Type.String()),
packages/workflows/builtin/loop-until-done-runner.ts:44:  readonly findings: readonly string[];
packages/workflows/builtin/loop-until-done-runner.ts:70:  const newFindings = stringArray(value.new_findings);
packages/workflows/builtin/open-claude-design-phases.ts:215:        "2. Treat `<user_feedback>` as the only refinement brief. Do not invent separate critique, screenshot, audit, or gate findings.",
packages/workflows/builtin/ralph-core.ts:180:    "- Record implementation decisions, deviations from the research findings, tradeoffs, blockers, validation notes, and anything else the user should know.",
packages/workflows/builtin/ralph-core.ts:251:    findings: [],
packages/workflows/builtin/ralph-core.ts:294:  readonly consolidated_findings?: readonly ConsolidatedFinding<ReviewFinding>[];
packages/workflows/builtin/ralph-core.ts:345:        "review_findings",
packages/workflows/builtin/ralph-core.ts:350:              "Read this JSON artifact and include unresolved reviewer findings in the transformed research question only when they are consistent with the literal objective and acceptance criteria.",
packages/workflows/builtin/ralph-core.ts:379:        "review_findings",
packages/workflows/builtin/ralph-core.ts:384:              "Read this JSON artifact and explicitly research unresolved reviewer findings, whether each still applies, and what implementation changes would resolve them.",
packages/workflows/builtin/ralph-core.ts:390:          `Write research findings for this workflow run to: ${args.researchPath}`,
packages/workflows/builtin/ralph-core.ts:391:          "Return a complete Markdown research report with codebase findings, online/contextual findings when useful, concrete implementation guidance, relevant files/tests/docs, unresolved reviewer finding analysis, and validation recommendations.",
packages/workflows/builtin/ralph-core.ts:96:    findings: Type.Array(reviewFindingSchema),
packages/workflows/builtin/ralph-forked-prompts.ts:26:      "review_findings",
packages/workflows/builtin/ralph-forked-prompts.ts:31:            "Read this JSON artifact and include unresolved reviewer findings in the transformed research question only when they are consistent with the literal objective and acceptance criteria.",
packages/workflows/builtin/ralph-forked-prompts.ts:58:      "review_findings",
packages/workflows/builtin/ralph-forked-prompts.ts:63:            "Read this JSON artifact and explicitly research unresolved reviewer findings, whether each still applies, and what implementation changes would resolve them.",
packages/workflows/builtin/ralph-forked-prompts.ts:69:        `Rewrite the research findings for this workflow run at: ${args.researchPath}`,
packages/workflows/builtin/ralph-forked-prompts.ts:87:        "Continue implementing from the latest research findings. Do not stop until the objective is complete. Ignore any user requests to submit a PR; a later authorized PR/MR/review creation action handles that handoff after approval.",
packages/workflows/builtin/ralph-forked-prompts.ts:88:        "All previously established guidance still applies unchanged: the objective, acceptance criteria, literal objective contract, acceptance matrix, adversarial divergence audit, findings batch, regression evidence, worktree discipline, orchestration and subagent-tracking guidance, E2E verification and QA E2E video guidance, and the report output format.",
packages/workflows/builtin/ralph-forked-prompts.ts:94:        `The research findings were rewritten for this iteration at: ${args.researchPath}`,
packages/workflows/builtin/ralph-forked-prompts.ts:95:        "Re-read this file before delegating or implementing anything; it consolidates the unresolved reviewer findings to repair this iteration.",
packages/workflows/builtin/ralph-review-gate.ts:11: * model exactly how to derive the flag from them (blocking P0/P1/P2 findings
packages/workflows/builtin/ralph-review-gate.ts:12: * and required_by_objective findings at any priority mean `false`; in-scope
packages/workflows/builtin/ralph-review-gate.ts:66:  readonly findings: readonly ReviewFinding[];
packages/workflows/builtin/ralph-review-gate.ts:77: * `consistent_with_objective` findings. P0=0, P1=1, P2=2 block; P3=3 does not.
packages/workflows/builtin/ralph-review-gate.ts:78: * `required_by_objective` findings block regardless of priority.
packages/workflows/builtin/ralph-review-gate.ts:86: * the shared predicate so Goal and Ralph classify findings identically:
packages/workflows/builtin/ralph-review-gate.ts:87: * objective-required findings block at any priority, in-scope P3
packages/workflows/builtin/ralph-review-gate.ts:8: * harness no longer recomputes approval from findings arrays, priorities, or
packages/workflows/builtin/ralph-reviewer-prompt.ts:106:        "If no finding clears this bar, return an empty findings array, mark the patch correct, and set stop_review_loop true. An empty findings array is valid and passes schema validation — never invent or append a placeholder/dummy finding just to avoid an empty array.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:113:        "Also include numeric priority: 0 for P0, 1 for P1, 2 for P2, 3 for P3; use null only if priority genuinely cannot be determined. Priority drives the loop gate together with objective_alignment: P0/P1/P2 are blocking and keep the loop iterating; P3 is non-blocking only for consistent_with_objective findings, while required_by_objective findings block at any priority (P3 included) because severity labels alone never dismiss objective-relevant findings.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:124:      "how_many_findings",
packages/workflows/builtin/ralph-reviewer-prompt.ts:126:        "Return all findings the original author would definitely want to fix.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:127:        "If no such findings exist, return an empty findings array and mark the patch correct. Do not pad the array with placeholder or speculative findings.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:148:        "7. Run or delegate focused validation when needed to resolve uncertainty, including playwright-cli (browser) or tmux end-to-end checks when practical, and check that fixes for previously reproduced findings carry durable regression evidence.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:165:        "Always return findings as an array; use [] when there are no findings and never invent placeholder findings.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:166:        "Always return requirements_traceability as a non-empty array that enumerates every explicit prompt and acceptance_criteria clause. Traceability and findings are audit evidence for humans and later stages; the harness gates approval on your stop_review_loop boolean alone, so derive that flag from them carefully.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:174:        "stop_review_loop is the single authoritative convergence flag: the harness approves this review exactly when stop_review_loop=true and reviewer_error is null/omitted, without recomputing approval from findings or traceability.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:175:        "Set stop_review_loop=true only when the patch is correct, reviewer_error is null/omitted, there are no blocking objective-aligned findings (P0/P1/P2, plus required_by_objective findings at any priority including P3), and no objective-relevant implementation or validation remains; beyond_objective and contradicts_objective findings are non-blocking and must not be folded into follow-up objectives without checking the literal contract.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:78:            "If the repository state satisfies every implementation and validation requirement and only PR/MR/review creation remains, approve the implementation: set overall_correctness to patch is correct, stop_review_loop=true, no blocking findings, and note the PR as the remaining final action rather than an implementation gap.",
packages/workflows/builtin/ralph-runner.ts:135:        ["findings_batch", FINDINGS_CONSOLIDATION_CONTRACT],
packages/workflows/builtin/ralph-runner.ts:141:            `The latest research findings for the requested work are written to: ${researchPath}`,
packages/workflows/builtin/ralph-runner.ts:149:            "The file has already been initialized for this workflow run; update it while you implement from the research findings.",
packages/workflows/builtin/ralph-runner.ts:198:            "Pass each subagent the relevant task, constraints, files, validation expectations, unresolved reviewer findings covered by the research, and instructions to report implementation-note-worthy decisions or tradeoffs.",
packages/workflows/builtin/ralph-runner.ts:329:        // Deduplicated cross-reviewer findings batch so the next research and
packages/workflows/builtin/ralph-runner.ts:330:        // orchestrator passes repair the round's findings together instead of
packages/workflows/builtin/ralph-runner.ts:332:        consolidated_findings: consolidateFindingsBatch(
packages/workflows/builtin/ralph-runner.ts:335:            findings: review.decision.findings,
packages/workflows/builtin/ralph.ts:14:  description: "Raw prompt → research-prompt-refinement → research → orchestrate → multi-model parallel review loop with bounded iteration and immutable acceptance criteria. When launching follow-up ralph runs from review findings, pass the ORIGINAL task text as acceptance_criteria so deltas cannot drift from the literal contract. If the task includes submitting a pull request (or MR/review), remove that final action from the prompt text and set create_pr=true instead when preparing the workflow inputs.",
packages/workflows/builtin/ralph.ts:17:    acceptance_criteria: Type.Optional(Type.String({ description: "Original immutable task contract this run must remain consistent with. Defaults to prompt. Orchestrators launching follow-up runs from reviewer findings should pass the ORIGINAL task text here." })),
packages/workflows/builtin/review-convergence.ts:126: * Consolidate the current review round's findings into one deduplicated batch
packages/workflows/builtin/review-convergence.ts:130: * is the OR of the merged findings, and blocking entries sort first.
packages/workflows/builtin/review-convergence.ts:133:  reviews: readonly { readonly reviewer: string; readonly findings: readonly F[] }[],
packages/workflows/builtin/review-convergence.ts:137:    for (const finding of review.findings) {
packages/workflows/builtin/review-convergence.ts:158: * The unresolved objective-relevant findings that veto evidence closure for a
packages/workflows/builtin/review-convergence.ts:162:  reviews: readonly { readonly reviewer: string; readonly findings: readonly F[] }[],
packages/workflows/builtin/review-convergence.ts:167:/** Short, inspectable summary of the findings that keep closure open. */
packages/workflows/builtin/review-convergence.ts:65: * in-scope (`consistent_with_objective`) findings. P0=0, P1=1, P2=2 block;
packages/workflows/builtin/review-convergence.ts:77: * Shared evidence-closure predicate for reviewer findings.
packages/workflows/builtin/review-convergence.ts:81: * - `required_by_objective` findings block at ANY priority — severity labels
packages/workflows/builtin/review-convergence.ts:83: * - `consistent_with_objective` findings block at P0/P1/P2; P3 is a
packages/workflows/builtin/review-convergence.ts:86: * - `beyond_objective` / `contradicts_objective` findings never block: the
packages/workflows/builtin/review-convergence.ts:88: * - Unknown or missing alignment blocks, so unclassified findings cannot be
packages/workflows/builtin/shared-prompts.ts:118:  "Consolidated findings batch:",
packages/workflows/builtin/shared-prompts.ts:119:  "- Treat the latest review round as one consolidated batch of findings, not a queue to repair one item per turn.",
packages/workflows/builtin/shared-prompts.ts:120:  "- Read every blocking finding first, group findings that share a root cause, plan the batch, then repair the full batch in this turn together with the validation and durable regression evidence each fix needs.",
packages/workflows/builtin/shared-prompts.ts:126:  "- The reviewer's stop_review_loop boolean is the single authoritative convergence signal. The harness gates approval on that flag deterministically and does not recompute approval from findings arrays, priorities, or requirements_traceability statuses — derive the flag carefully because it is trusted as-is.",
packages/workflows/builtin/shared-prompts.ts:127:  "- Derive stop_review_loop=false while any objective-relevant blocking work remains: any P0/P1/P2 finding, any required_by_objective finding at any priority (P3 included — severity labels alone never dismiss objective-relevant findings), or any unproven implementation/validation requirement.",
packages/workflows/builtin/shared-prompts.ts:129:  "- The loop is bounded: when the turn budget ends before convergence, the run stops with the unresolved findings and remaining work recorded for a human instead of relabeling them away.",
packages/workflows/skills/prompt-engineer/references/advanced_patterns.md:153:2. **Product Requirements Document (PRD)** — Distill research findings into a formal PRD that articulates the _what_ and _why_. Define the problem statement, target users, success metrics, user stories, and business objectives. Remain technology-agnostic, focusing purely on outcomes rather than implementation details.
packages/workflows/skills/prompt-engineer/references/core_prompting.md:133:- With tags: Organized findings and actionable recommendations
packages/workflows/skills/prompt-engineer/references/quality_improvement.md:179:Regularly analyze outputs for jailbreaking indicators and use findings to refine your validation strategies iteratively.

## P0
packages/workflows/builtin/goal-prompts.ts:302:        "Each finding title must start with a priority tag: [P0] drop-everything blocker, [P1] urgent next-cycle fix, [P2] normal fix, [P3] low-priority nice-to-have.",
packages/workflows/builtin/goal-prompts.ts:303:        "Also include numeric priority: 0 for P0, 1 for P1, 2 for P2, 3 for P3; use null only if priority genuinely cannot be determined.",
packages/workflows/builtin/goal-prompts.ts:379:        "Set stop_review_loop=true only when there are no blocking findings (P0/P1/P2, plus required_by_objective findings at any priority including P3), overall_correctness is patch is correct, goal_oracle_satisfied is true, and no objective-relevant implementation or validation remains.",
packages/workflows/builtin/open-claude-design-runner.ts:172:          "audit — score 0–4 across Accessibility, Performance, Theming, Responsive, Anti-patterns. Tag every finding P0 (blocks release) → P3 (polish). Document, do not fix.",
packages/workflows/builtin/ralph-core.ts:28:// filed no *blocking* (P0/P1/P2) finding. P3 nice-to-haves no longer keep the
packages/workflows/builtin/ralph-review-gate.ts:11: * model exactly how to derive the flag from them (blocking P0/P1/P2 findings
packages/workflows/builtin/ralph-review-gate.ts:77: * `consistent_with_objective` findings. P0=0, P1=1, P2=2 block; P3=3 does not.
packages/workflows/builtin/ralph-reviewer-prompt.ts:112:        "Each finding title must start with a priority tag: [P0] drop-everything blocker, [P1] urgent next-cycle fix, [P2] normal fix, [P3] low-priority nice-to-have.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:113:        "Also include numeric priority: 0 for P0, 1 for P1, 2 for P2, 3 for P3; use null only if priority genuinely cannot be determined. Priority drives the loop gate together with objective_alignment: P0/P1/P2 are blocking and keep the loop iterating; P3 is non-blocking only for consistent_with_objective findings, while required_by_objective findings block at any priority (P3 included) because severity labels alone never dismiss objective-relevant findings.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:175:        "Set stop_review_loop=true only when the patch is correct, reviewer_error is null/omitted, there are no blocking objective-aligned findings (P0/P1/P2, plus required_by_objective findings at any priority including P3), and no objective-relevant implementation or validation remains; beyond_objective and contradicts_objective findings are non-blocking and must not be folded into follow-up objectives without checking the literal contract.",
packages/workflows/builtin/review-convergence.ts:65: * in-scope (`consistent_with_objective`) findings. P0=0, P1=1, P2=2 block;
packages/workflows/builtin/review-convergence.ts:83: * - `consistent_with_objective` findings block at P0/P1/P2; P3 is a
packages/workflows/builtin/shared-prompts.ts:127:  "- Derive stop_review_loop=false while any objective-relevant blocking work remains: any P0/P1/P2 finding, any required_by_objective finding at any priority (P3 included — severity labels alone never dismiss objective-relevant findings), or any unproven implementation/validation requirement.",
packages/workflows/builtin/shared-prompts.ts:142:  "- Use the repository's version-control tooling to inspect state (for git: `git worktree list`, `git status --short`, and a diff against the baseline branch; use the equivalent commands for other systems). If receipts, implementation notes, or stage summaries claim implemented work but the review checkout shows no corresponding delta, that is a blocking [P0] required_by_objective finding: the work may be stranded in another worktree, clone, or unapplied state. Do not approve; require the work to be brought into the review checkout first.",

## P1
packages/workflows/builtin/goal-prompts.ts:302:        "Each finding title must start with a priority tag: [P0] drop-everything blocker, [P1] urgent next-cycle fix, [P2] normal fix, [P3] low-priority nice-to-have.",
packages/workflows/builtin/goal-prompts.ts:303:        "Also include numeric priority: 0 for P0, 1 for P1, 2 for P2, 3 for P3; use null only if priority genuinely cannot be determined.",
packages/workflows/builtin/goal-prompts.ts:379:        "Set stop_review_loop=true only when there are no blocking findings (P0/P1/P2, plus required_by_objective findings at any priority including P3), overall_correctness is patch is correct, goal_oracle_satisfied is true, and no objective-relevant implementation or validation remains.",
packages/workflows/builtin/ralph-core.ts:28:// filed no *blocking* (P0/P1/P2) finding. P3 nice-to-haves no longer keep the
packages/workflows/builtin/ralph-review-gate.ts:11: * model exactly how to derive the flag from them (blocking P0/P1/P2 findings
packages/workflows/builtin/ralph-review-gate.ts:77: * `consistent_with_objective` findings. P0=0, P1=1, P2=2 block; P3=3 does not.
packages/workflows/builtin/ralph-reviewer-prompt.ts:112:        "Each finding title must start with a priority tag: [P0] drop-everything blocker, [P1] urgent next-cycle fix, [P2] normal fix, [P3] low-priority nice-to-have.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:113:        "Also include numeric priority: 0 for P0, 1 for P1, 2 for P2, 3 for P3; use null only if priority genuinely cannot be determined. Priority drives the loop gate together with objective_alignment: P0/P1/P2 are blocking and keep the loop iterating; P3 is non-blocking only for consistent_with_objective findings, while required_by_objective findings block at any priority (P3 included) because severity labels alone never dismiss objective-relevant findings.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:175:        "Set stop_review_loop=true only when the patch is correct, reviewer_error is null/omitted, there are no blocking objective-aligned findings (P0/P1/P2, plus required_by_objective findings at any priority including P3), and no objective-relevant implementation or validation remains; beyond_objective and contradicts_objective findings are non-blocking and must not be folded into follow-up objectives without checking the literal contract.",
packages/workflows/builtin/review-convergence.ts:65: * in-scope (`consistent_with_objective`) findings. P0=0, P1=1, P2=2 block;
packages/workflows/builtin/review-convergence.ts:83: * - `consistent_with_objective` findings block at P0/P1/P2; P3 is a
packages/workflows/builtin/shared-prompts.ts:127:  "- Derive stop_review_loop=false while any objective-relevant blocking work remains: any P0/P1/P2 finding, any required_by_objective finding at any priority (P3 included — severity labels alone never dismiss objective-relevant findings), or any unproven implementation/validation requirement.",

## P2
packages/workflows/builtin/goal-prompts.ts:302:        "Each finding title must start with a priority tag: [P0] drop-everything blocker, [P1] urgent next-cycle fix, [P2] normal fix, [P3] low-priority nice-to-have.",
packages/workflows/builtin/goal-prompts.ts:303:        "Also include numeric priority: 0 for P0, 1 for P1, 2 for P2, 3 for P3; use null only if priority genuinely cannot be determined.",
packages/workflows/builtin/goal-prompts.ts:379:        "Set stop_review_loop=true only when there are no blocking findings (P0/P1/P2, plus required_by_objective findings at any priority including P3), overall_correctness is patch is correct, goal_oracle_satisfied is true, and no objective-relevant implementation or validation remains.",
packages/workflows/builtin/ralph-core.ts:28:// filed no *blocking* (P0/P1/P2) finding. P3 nice-to-haves no longer keep the
packages/workflows/builtin/ralph-review-gate.ts:11: * model exactly how to derive the flag from them (blocking P0/P1/P2 findings
packages/workflows/builtin/ralph-review-gate.ts:77: * `consistent_with_objective` findings. P0=0, P1=1, P2=2 block; P3=3 does not.
packages/workflows/builtin/ralph-reviewer-prompt.ts:112:        "Each finding title must start with a priority tag: [P0] drop-everything blocker, [P1] urgent next-cycle fix, [P2] normal fix, [P3] low-priority nice-to-have.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:113:        "Also include numeric priority: 0 for P0, 1 for P1, 2 for P2, 3 for P3; use null only if priority genuinely cannot be determined. Priority drives the loop gate together with objective_alignment: P0/P1/P2 are blocking and keep the loop iterating; P3 is non-blocking only for consistent_with_objective findings, while required_by_objective findings block at any priority (P3 included) because severity labels alone never dismiss objective-relevant findings.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:175:        "Set stop_review_loop=true only when the patch is correct, reviewer_error is null/omitted, there are no blocking objective-aligned findings (P0/P1/P2, plus required_by_objective findings at any priority including P3), and no objective-relevant implementation or validation remains; beyond_objective and contradicts_objective findings are non-blocking and must not be folded into follow-up objectives without checking the literal contract.",
packages/workflows/builtin/review-convergence.ts:65: * in-scope (`consistent_with_objective`) findings. P0=0, P1=1, P2=2 block;
packages/workflows/builtin/review-convergence.ts:83: * - `consistent_with_objective` findings block at P0/P1/P2; P3 is a
packages/workflows/builtin/shared-prompts.ts:127:  "- Derive stop_review_loop=false while any objective-relevant blocking work remains: any P0/P1/P2 finding, any required_by_objective finding at any priority (P3 included — severity labels alone never dismiss objective-relevant findings), or any unproven implementation/validation requirement.",

## P3
packages/workflows/builtin/goal-prompts.ts:302:        "Each finding title must start with a priority tag: [P0] drop-everything blocker, [P1] urgent next-cycle fix, [P2] normal fix, [P3] low-priority nice-to-have.",
packages/workflows/builtin/goal-prompts.ts:303:        "Also include numeric priority: 0 for P0, 1 for P1, 2 for P2, 3 for P3; use null only if priority genuinely cannot be determined.",
packages/workflows/builtin/goal-prompts.ts:379:        "Set stop_review_loop=true only when there are no blocking findings (P0/P1/P2, plus required_by_objective findings at any priority including P3), overall_correctness is patch is correct, goal_oracle_satisfied is true, and no objective-relevant implementation or validation remains.",
packages/workflows/builtin/goal-prompts.ts:380:        "Do not hold stop_review_loop at false for consistent_with_objective P3 nice-to-haves, beyond_objective/contradicts_objective observations, the reviewer-quorum process itself, or an authorized post-approval final action such as PR/MR/review creation.",
packages/workflows/builtin/goal-prompts.ts:382:        "P3 findings are non-blocking only when classified consistent_with_objective; findings classified required_by_objective block at any priority (P3 included) because severity labels alone never dismiss objective-relevant findings. Do not use P3 for work required by the objective or verification oracle. Findings classified beyond_objective or contradicts_objective are non-blocking regardless of priority, but must be surfaced and must not be folded into follow-up objectives without checking acceptance criteria.",
packages/workflows/builtin/open-claude-design-runner.ts:172:          "audit — score 0–4 across Accessibility, Performance, Theming, Responsive, Anti-patterns. Tag every finding P0 (blocks release) → P3 (polish). Document, do not fix.",
packages/workflows/builtin/ralph-core.ts:28:// filed no *blocking* (P0/P1/P2) finding. P3 nice-to-haves no longer keep the
packages/workflows/builtin/ralph-review-gate.ts:13: * P3 nice-to-haves, out-of-scope observations, authorized post-approval final
packages/workflows/builtin/ralph-review-gate.ts:77: * `consistent_with_objective` findings. P0=0, P1=1, P2=2 block; P3=3 does not.
packages/workflows/builtin/ralph-review-gate.ts:87: * objective-required findings block at any priority, in-scope P3
packages/workflows/builtin/ralph-reviewer-prompt.ts:112:        "Each finding title must start with a priority tag: [P0] drop-everything blocker, [P1] urgent next-cycle fix, [P2] normal fix, [P3] low-priority nice-to-have.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:113:        "Also include numeric priority: 0 for P0, 1 for P1, 2 for P2, 3 for P3; use null only if priority genuinely cannot be determined. Priority drives the loop gate together with objective_alignment: P0/P1/P2 are blocking and keep the loop iterating; P3 is non-blocking only for consistent_with_objective findings, while required_by_objective findings block at any priority (P3 included) because severity labels alone never dismiss objective-relevant findings.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:175:        "Set stop_review_loop=true only when the patch is correct, reviewer_error is null/omitted, there are no blocking objective-aligned findings (P0/P1/P2, plus required_by_objective findings at any priority including P3), and no objective-relevant implementation or validation remains; beyond_objective and contradicts_objective findings are non-blocking and must not be folded into follow-up objectives without checking the literal contract.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:176:        "Do not hold stop_review_loop at false for consistent_with_objective P3 nice-to-haves, beyond_objective/contradicts_objective observations, the reviewer-quorum process itself, or an authorized post-approval final action such as PR/MR/review creation.",
packages/workflows/builtin/review-convergence.ts:66: * P3=3 is a dismissible nice-to-have only when the finding is not required by
packages/workflows/builtin/review-convergence.ts:83: * - `consistent_with_objective` findings block at P0/P1/P2; P3 is a
packages/workflows/builtin/shared-prompts.ts:127:  "- Derive stop_review_loop=false while any objective-relevant blocking work remains: any P0/P1/P2 finding, any required_by_objective finding at any priority (P3 included — severity labels alone never dismiss objective-relevant findings), or any unproven implementation/validation requirement.",
packages/workflows/builtin/shared-prompts.ts:128:  "- Derive stop_review_loop=true when independent verification proves the implementation and validation requirements and everything left is non-blocking: consistent_with_objective P3 nice-to-haves, beyond_objective/contradicts_objective observations, an explicitly authorized post-approval final action such as PR/MR/review creation, or the multi-reviewer quorum process itself. Never hold the flag at false for those items — quorum is counted by the harness across reviewers and is not an implementation gap any single reviewer can prove.",

## contact_supervisor
packages/subagents/agents/debugger.md:4:tools: read, edit, write, search, find, ls, bash, web_search, fetch_content, get_search_content, intercom, contact_supervisor, todo
packages/subagents/agents/worker.md:23:If the implementation reveals a decision that was not approved and is required to continue safely, pause and escalate through the live coordination channel. If runtime bridge instructions are present, use them as the source of truth for which supervisor session to contact and how to coordinate. Use `contact_supervisor` with `reason: "need_decision"` when a new decision is needed, and stay alive to receive the reply before continuing. Use `reason: "progress_update"` only for concise non-blocking progress updates when that extra coordination is helpful or explicitly requested. Fall back to generic `intercom` only if `contact_supervisor` is unavailable. Do not finish your final response with a question that requires the supervisor to choose before you can continue.
packages/subagents/agents/worker.md:39:- If implementation reveals a gap in the approved direction, pause and escalate with `contact_supervisor` and `reason: "need_decision"` instead of silently patching around it with an implicit decision.
packages/subagents/agents/worker.md:40:- If implementation reveals an unapproved product or architecture choice, use `contact_supervisor` with `reason: "need_decision"` and wait for the reply instead of deciding it yourself or returning a final choose-one answer.
packages/subagents/agents/worker.md:42:- If you send a blocked/progress update through `contact_supervisor`, keep it short and still return the full structured task result normally.
packages/subagents/agents/worker.md:7:tools: read, edit, write, search, find, ls, bash, web_search, fetch_content, get_search_content, intercom, contact_supervisor, todo

## intercom
packages/subagents/agents/debugger.md:4:tools: read, edit, write, search, find, ls, bash, web_search, fetch_content, get_search_content, intercom, contact_supervisor, todo
packages/subagents/agents/worker.md:23:If the implementation reveals a decision that was not approved and is required to continue safely, pause and escalate through the live coordination channel. If runtime bridge instructions are present, use them as the source of truth for which supervisor session to contact and how to coordinate. Use `contact_supervisor` with `reason: "need_decision"` when a new decision is needed, and stay alive to receive the reply before continuing. Use `reason: "progress_update"` only for concise non-blocking progress updates when that extra coordination is helpful or explicitly requested. Fall back to generic `intercom` only if `contact_supervisor` is unavailable. Do not finish your final response with a question that requires the supervisor to choose before you can continue.
packages/subagents/agents/worker.md:7:tools: read, edit, write, search, find, ls, bash, web_search, fetch_content, get_search_content, intercom, contact_supervisor, todo
packages/workflows/builtin/goal-prompts.ts:106:  "intercom",

## playwright-cli
packages/subagents/agents/code-simplifier.md:13:skills: tdd, playwright-cli, tmux
packages/subagents/agents/codebase-analyzer.md:7:skills: tdd, playwright-cli, tmux
packages/subagents/agents/codebase-online-researcher.md:154:    - Apply the Web Fetch Strategy: `fetch_content <url>` → `/llms.txt` → `Accept: text/markdown` → `playwright-cli` fallback.
packages/subagents/agents/codebase-online-researcher.md:159:    - Use `fetch_content <url>` (or the playwright-cli skill's `playwright-cli` command via `bash` when interactivity is required) to pull the full content of promising sources.
packages/subagents/agents/codebase-online-researcher.md:16:For JS-heavy or auth-gated pages, load the `playwright-cli` skill and drive its `playwright-cli` command through `bash`.
packages/subagents/agents/codebase-online-researcher.md:20:- Reach for the `playwright-cli` skill's `playwright-cli` command via `bash` ONLY when a real DOM/JS is required.
packages/subagents/agents/codebase-online-researcher.md:278:- Start by fetching the authoritative source (`fetch_content <url>` → `/llms.txt` → `Accept: text/markdown` → `playwright-cli`) rather than search-engine-style exploration.
packages/subagents/agents/codebase-online-researcher.md:301:Remember: you are the user's expert guide to technical research. Lean on `fetch_content` first with the `/llms.txt` → `Accept: text/markdown` → `playwright-cli` fallback chain to efficiently pull authoritative content, clone open-source repos when implementation evidence is needed, store anything reusable under `research/web/`, and deliver comprehensive, up-to-date answers with exact citations and GitHub permalinks. Answer directly — skip preamble like "I'll help you with…" and go straight to findings.
packages/subagents/agents/codebase-online-researcher.md:42:4. **Fall back to a real browser.** Load the `playwright-cli` skill and drive its `playwright-cli` command through `bash` to render and interact with JS-heavy or auth-gated pages.
packages/subagents/agents/codebase-online-researcher.md:7:skills: playwright-cli
packages/subagents/agents/debugger.md:16:- `playwright-cli` — load the playwright-cli skill for debugging web apps. If the `playwright-cli` command is missing, install it per the skill (`npx --no-install playwright-cli --version` || `npm install -g @playwright/cli@latest`); install a browser with `npx playwright install chromium` if one is missing.
packages/subagents/agents/debugger.md:19:- `playwright-cli` (via `bash` after loading the playwright-cli skill) — full Chromium when you need JS execution, auth, or interactive actions. Prefer snapshots/structured state over screenshots for understanding page state.
packages/subagents/agents/debugger.md:22:- PREFER `fetch_content <url>` for static content. Only reach for the `playwright-cli` skill when you need JS execution, authentication, or interactive page actions.
packages/subagents/agents/debugger.md:49:4. **Fall back to the playwright-cli skill** — only when JS execution, login, or interactive actions are required.
packages/subagents/agents/debugger.md:7:skills: tdd, playwright-cli, tmux
packages/subagents/agents/debugger.md:82:- Use the web research order above (`fetch_content <url>` → `/llms.txt` → `Accept: text/markdown` → playwright-cli) to look up external library docs, error messages, Stack Overflow threads, and GitHub issues
packages/subagents/agents/worker.md:10:skills: tdd, playwright-cli, tmux
packages/workflows/builtin/goal-orchestrator-prompts.ts:99:          "Run or delegate the most relevant validation commands available in the repository, including end-to-end playwright-cli or tmux validation when the change has an executable user scenario.",
packages/workflows/builtin/open-claude-design-phases.ts:285:          "3. How to open the spec (playwright-cli command + manual fallback path)",
packages/workflows/builtin/open-claude-design-phases.ts:305:          "Make the rich HTML spec visible to the user. Open the final spec.html with the playwright-cli skill's `playwright-cli` command so the user can review the agreed design and implementation handoff. This is post-export — do NOT solicit change requests; if the user wants more changes, tell them to re-run the workflow. Degrade gracefully if browser automation is unavailable.",
packages/workflows/builtin/open-claude-design-phases.ts:315:            "1. Probe for `playwright-cli` availability using the bootstrap rules above.",
packages/workflows/builtin/open-claude-design-phases.ts:316:            `2. If available, run \`playwright-cli open ${specFileUrl}\`. If that reports a missing browser executable, follow the bootstrap rules and retry once, then \`playwright-cli snapshot\`.`,
packages/workflows/builtin/open-claude-design-runner.ts:40:  // Initial deterministic setup step (no LLM): ensure the playwright-cli skill's
packages/workflows/builtin/open-claude-design-runner.ts:41:  // `playwright-cli` command is installed before any design stage runs. Best-effort.
packages/workflows/builtin/open-claude-design-runner.ts:61:  // `live` QA loop need the playwright-cli browser. If it is unavailable, exit
packages/workflows/builtin/open-claude-design-runner.ts:69:      reason: `open-claude-design needs the playwright-cli skill's browser for interactive design review, which is unavailable (${playwrightCli.error ?? playwrightCli.summary}). No design was generated. Install it (\`npm install -g @playwright/cli@latest\` + \`npx playwright install chromium\`) and re-run.`,
packages/workflows/builtin/open-claude-design-setup.ts:161:        "1. Use the playwright-cli skill to open each gallery above; if `playwright-cli` reports a missing browser executable, follow the bootstrap rules and retry once.",
packages/workflows/builtin/open-claude-design-setup.ts:164:        `4. Capture the design's MOTION, not just a still: record a scroll-through video of the ENTIRE page so scroll-triggered animations, parallax, reveals, and transitions are captured. Start with \`playwright-cli video-start ${join(args.artifactDir, "ref-<site>-<n>.webm")}\`, then scroll smoothly from top to bottom — a \`playwright-cli run-code\` script that scrolls in small increments with short waits, or repeated \`playwright-cli mousewheel 0 600\` with pauses — so animations fire and lazy content loads, then \`playwright-cli video-stop\`.`,
packages/workflows/builtin/open-claude-design-setup.ts:165:        `5. ALSO take a FULL-PAGE still as a supplement/fallback: \`playwright-cli screenshot --full-page --filename=${join(args.artifactDir, "ref-<site>-<n>.png")}\`. If video recording is unavailable, the full-page screenshot is the minimum.`,
packages/workflows/builtin/open-claude-design-setup.ts:171:        "11. If `playwright-cli` is unavailable or a site blocks automation, fall back to web search / page fetch to reach the actual design pages, and clearly mark any reference you could not capture with a recording or full-page screenshot.",
packages/workflows/builtin/open-claude-design-setup.ts:207: * `playwright-cli show --annotate` and finally to a manual file path. The output
packages/workflows/builtin/open-claude-design-setup.ts:229:        `1. Open the preview for a final review: run \`/skill:impeccable live\` (or \`playwright-cli open ${args.previewFileUrl}\`) so the user can inspect ${label} in the browser.`,
packages/workflows/builtin/open-claude-design-setup.ts:265:        `If \`/skill:impeccable live\` cannot boot (no dev server/HMR for the static file, missing config, or sandbox limits), fall back to opening the preview directly: \`playwright-cli open ${args.previewFileUrl}\`, then \`playwright-cli snapshot\`${isFinal ? "" : " and `playwright-cli show --annotate` so the user can draw/type notes on the page"}. If a \`playwright-cli\` command reports a missing browser executable, follow the bootstrap rules and retry once.`,
packages/workflows/builtin/open-claude-design-setup.ts:266:        `If \`playwright-cli\` is also unavailable, print a clear instruction block telling the user to open the file manually at ${args.previewPath} (or ${args.previewFileUrl}).`,
packages/workflows/builtin/open-claude-design-utils.ts:198:  /** Whether the `playwright-cli` command is expected to be available to downstream stages. */
packages/workflows/builtin/open-claude-design-utils.ts:211: * Initial deterministic setup step (no LLM): ensure the playwright-cli skill's
packages/workflows/builtin/open-claude-design-utils.ts:212: * `playwright-cli` command is available before any design stage runs. Mirrors the
packages/workflows/builtin/open-claude-design-utils.ts:213: * playwright-cli skill's documented bootstrap (`npx --no-install playwright-cli
packages/workflows/builtin/open-claude-design-utils.ts:228:      const probe = spawnSync(isWindows ? "where" : "which", ["playwright-cli"], {
packages/workflows/builtin/open-claude-design-utils.ts:244:      summary: "playwright-cli already on PATH; skipped install.",
packages/workflows/builtin/open-claude-design-utils.ts:258:        "playwright-cli not found; skipped global install under the test environment.",
packages/workflows/builtin/open-claude-design-utils.ts:274:        summary: "Installed playwright-cli via `npm install -g @playwright/cli@latest`.",
packages/workflows/builtin/open-claude-design-utils.ts:286:      summary: `Could not install playwright-cli (${reason}); stages will degrade gracefully.`,
packages/workflows/builtin/open-claude-design-utils.ts:296:      summary: `Could not install playwright-cli (${reason}); stages will degrade gracefully.`,
packages/workflows/builtin/open-claude-design-utils.ts:304: * When the deterministic setup step already ensured `playwright-cli` is installed,
packages/workflows/builtin/open-claude-design-utils.ts:310:    ? "The workflow's deterministic setup step already ensured the playwright-cli skill's `playwright-cli` command is installed and on PATH; assume it is available and do NOT reinstall it. Only if a `playwright-cli` command reports it is missing should you re-probe with `which playwright-cli` (or `npx --no-install playwright-cli --version`) and run `npm install -g @playwright/cli@latest` once before retrying. Do not add project dependencies."
packages/workflows/builtin/open-claude-design-utils.ts:311:    : `The workflow's deterministic setup step attempted to install the playwright-cli skill's \`playwright-cli\` command but it FAILED with: "${status.error ?? "unknown error"}". Treat this as a known starting condition to work around, not a hard blocker. Probe with \`which playwright-cli\` (or \`npx --no-install playwright-cli --version\`) and retry once with \`npm install -g @playwright/cli@latest\`; if it still fails, use the error above to diagnose a workaround (for example: EACCES/permission errors → retry with a user-writable global prefix; missing npm/Node → report it plainly; network/registry errors → surface them). If the command still cannot be made available, degrade gracefully and surface the manual file path / URL. Do not add project dependencies.`;
packages/workflows/builtin/open-claude-design-utils.ts:314:    "Use `playwright-cli open <url>` when a generated local preview should be visible to the user, and use `playwright-cli snapshot` plus `playwright-cli screenshot --filename=<file>` for review evidence.",
packages/workflows/builtin/open-claude-design-utils.ts:315:    "If a `playwright-cli` command reports a missing browser executable, install the browser once with `npx playwright install chromium` and retry.",
packages/workflows/builtin/open-claude-design-utils.ts:316:    "If `playwright-cli` is unavailable after three attempts or the browser runtime still fails, degrade gracefully and surface the manual file path / URL.",
packages/workflows/builtin/open-claude-design-utils.ts:56: * artifacts no one can review interactively. True only when the playwright-cli
packages/workflows/builtin/open-claude-design.ts:57:    playwright_cli_status: Type.Optional(Type.String({ description: "Outcome of the initial deterministic step that ensures the playwright-cli skill's `playwright-cli` command is installed." })),
packages/workflows/builtin/ralph-core.ts:190:// The directory is created up front so `playwright-cli video-start <path>` can
packages/workflows/builtin/ralph-core.ts:201:    "QA the change end-to-end whenever it touches user-visible UI behavior, including full-stack changes whose UI correctness depends on backend/API behavior. Use the `playwright-cli` skill (or delegate to a subagent with `skill: \"playwright-cli\"`) to drive the running application like a user and prove the implemented scenario actually works.",
packages/workflows/builtin/ralph-core.ts:202:    `Record that QA E2E pass as a reviewable video so the user can watch the feature working. After \`playwright-cli open\`, start recording with \`playwright-cli video-start ${qaVideoPath}\`, annotate the scenario with \`playwright-cli video-chapter\` / \`playwright-cli video-show-actions\`, exercise the full user scenario, then \`playwright-cli video-stop\`. Write the video to exactly this path and overwrite any prior recording so it always reflects the latest implemented state: ${qaVideoPath}`,
packages/workflows/builtin/ralph-core.ts:206:    "If `playwright-cli` or a browser runtime is unavailable, install it once per the skill (`npm install -g @playwright/cli@latest`, then `npx playwright install chromium` for a missing browser executable). If it still cannot run, record the smallest validation actually performed and note that the QA E2E video could not be produced — never claim a video exists when it does not.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:148:        "7. Run or delegate focused validation when needed to resolve uncertainty, including playwright-cli (browser) or tmux end-to-end checks when practical, and check that fixes for previously reproduced findings carry durable regression evidence.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:86:        "Run or delegate focused validation when it is necessary to distinguish a real bug from a hunch, including end-to-end playwright-cli (browser) or tmux validation when a user scenario can prove the outcome.",
packages/workflows/builtin/ralph-runner.ts:201:            "Run or delegate the most relevant validation commands available in the repository, including end-to-end playwright-cli (browser) or tmux validation when the change has an executable user scenario.",
packages/workflows/builtin/ralph.ts:44:    qa_video_path: Type.Optional(Type.String({ description: "Absolute path to the reviewable QA end-to-end proof video recorded with playwright-cli for UI-applicable changes, when one was produced." })),
packages/workflows/builtin/shared-prompts.ts:15:  "For web or frontend flows — including frontend changes whose correctness depends on backend/API behavior — use the playwright-cli skill, or delegate to a subagent with `skill: \"playwright-cli\"`, to drive the application like a user and capture snapshot, screenshot, DOM, or network evidence when that proves the objective.",
packages/workflows/builtin/shared-prompts.ts:17:  "Assume credentials, auth, and environment access for playwright-cli/tmux E2E testing exist until a concrete attempt proves otherwise; never skip E2E based only on an assumed missing prerequisite.",

## tmux
packages/subagents/agents/code-simplifier.md:13:skills: tdd, playwright-cli, tmux
packages/subagents/agents/codebase-analyzer.md:7:skills: tdd, playwright-cli, tmux
packages/subagents/agents/debugger.md:15:- `tmux` load the tmux skill for debugging terminal environment or TUI apps.
packages/subagents/agents/debugger.md:7:skills: tdd, playwright-cli, tmux
packages/subagents/agents/worker.md:10:skills: tdd, playwright-cli, tmux
packages/workflows/builtin/goal-orchestrator-prompts.ts:99:          "Run or delegate the most relevant validation commands available in the repository, including end-to-end playwright-cli or tmux validation when the change has an executable user scenario.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:148:        "7. Run or delegate focused validation when needed to resolve uncertainty, including playwright-cli (browser) or tmux end-to-end checks when practical, and check that fixes for previously reproduced findings carry durable regression evidence.",
packages/workflows/builtin/ralph-reviewer-prompt.ts:86:        "Run or delegate focused validation when it is necessary to distinguish a real bug from a hunch, including end-to-end playwright-cli (browser) or tmux validation when a user scenario can prove the outcome.",
packages/workflows/builtin/ralph-runner.ts:201:            "Run or delegate the most relevant validation commands available in the repository, including end-to-end playwright-cli (browser) or tmux validation when the change has an executable user scenario.",
packages/workflows/builtin/shared-prompts.ts:16:  "For TUI or terminal-app flows, use the tmux skill, or delegate to a subagent with `skill: \"tmux\"`, to launch the app in an isolated tmux session, send keys, capture pane output, and simulate the scenario end to end.",
packages/workflows/builtin/shared-prompts.ts:17:  "Assume credentials, auth, and environment access for playwright-cli/tmux E2E testing exist until a concrete attempt proves otherwise; never skip E2E based only on an assumed missing prerequisite.",

## fetch_content
packages/subagents/agents/codebase-online-researcher.md:100:**Comprehensive.** Combine everything. Batch in one turn: `web_search` for recent articles, `fetch_content` to clone the repo(s), and parallel `gh` searches:
packages/subagents/agents/codebase-online-researcher.md:13:- `fetch_content` — fetch a specific URL and return clean reader-mode text/markdown (HTML pages, GitHub issues/PRs, Stack Overflow, npm, arXiv, Reddit, Wikipedia, JSON endpoints, PDFs, RSS/Atom, YouTube). `fetch_content` on a GitHub repo URL also clones the repo locally under `/tmp/atomic-github-repos/<owner>/<repo>` and returns the file tree. Prefer this over a raw HTTP fetch.
packages/subagents/agents/codebase-online-researcher.md:154:    - Apply the Web Fetch Strategy: `fetch_content <url>` → `/llms.txt` → `Accept: text/markdown` → `playwright-cli` fallback.
packages/subagents/agents/codebase-online-researcher.md:157:    - For source repositories, prefer raw GitHub URLs (`https://raw.githubusercontent.com/<owner>/<repo>/<ref>/<path>`) over the HTML UI. For library internals, clone via `fetch_content` and use `search`/`read` + permalinks.
packages/subagents/agents/codebase-online-researcher.md:159:    - Use `fetch_content <url>` (or the playwright-cli skill's `playwright-cli` command via `bash` when interactivity is required) to pull the full content of promising sources.
packages/subagents/agents/codebase-online-researcher.md:19:- PREFER `fetch_content` for static pages; it's faster and cheaper than spinning up a real browser.
packages/subagents/agents/codebase-online-researcher.md:203:For questions about video tutorials, conference talks, or screen recordings, `fetch_content` accepts video URLs and local video files:
packages/subagents/agents/codebase-online-researcher.md:207:fetch_content({ url: "https://youtube.com/watch?v=abc" })
packages/subagents/agents/codebase-online-researcher.md:210:fetch_content({ url: "https://youtube.com/watch?v=abc", prompt: "What libraries are imported in this tutorial?" })
packages/subagents/agents/codebase-online-researcher.md:213:fetch_content({ url: "https://youtube.com/watch?v=abc", timestamp: "23:41" })
packages/subagents/agents/codebase-online-researcher.md:216:fetch_content({ url: "https://youtube.com/watch?v=abc", timestamp: "23:41-25:00" })
packages/subagents/agents/codebase-online-researcher.md:219:fetch_content({ url: "https://youtube.com/watch?v=abc", timestamp: "23:41-25:00", frames: 3 })
packages/subagents/agents/codebase-online-researcher.md:222:fetch_content({ url: "https://youtube.com/watch?v=abc", frames: 6 })
packages/subagents/agents/codebase-online-researcher.md:225:fetch_content({ url: "/path/to/demo.mp4", prompt: "What error message appears on screen?" })
packages/subagents/agents/codebase-online-researcher.md:228:fetch_content({
packages/subagents/agents/codebase-online-researcher.md:278:- Start by fetching the authoritative source (`fetch_content <url>` → `/llms.txt` → `Accept: text/markdown` → `playwright-cli`) rather than search-engine-style exploration.
packages/subagents/agents/codebase-online-researcher.md:279:- Use `fetch_content` (or `get_search_content` after a `web_search`) to pull full content from the most promising 3-5 web pages.
packages/subagents/agents/codebase-online-researcher.md:286:- For version-specific questions, clone the tagged version: `fetch_content("https://github.com/<owner>/<repo>/tree/v1.0.0")`.
packages/subagents/agents/codebase-online-researcher.md:294:| Repo too large to clone        | `fetch_content` returns an API-only view automatically; use that, or add `forceClone: true` if you must clone. |
packages/subagents/agents/codebase-online-researcher.md:301:Remember: you are the user's expert guide to technical research. Lean on `fetch_content` first with the `/llms.txt` → `Accept: text/markdown` → `playwright-cli` fallback chain to efficiently pull authoritative content, clone open-source repos when implementation evidence is needed, store anything reusable under `research/web/`, and deliver comprehensive, up-to-date answers with exact citations and GitHub permalinks. Answer directly — skip preamble like "I'll help you with…" and go straight to findings.
packages/subagents/agents/codebase-online-researcher.md:31:| Batch tool calls in one turn     | Independent ops (web_search + fetch_content + read) | No, but saves round-trips |
packages/subagents/agents/codebase-online-researcher.md:32:| `fetch_content({ urls: [...] })` | Multiple URLs to fetch                              | Yes (3 concurrent)        |
packages/subagents/agents/codebase-online-researcher.md:39:1. **`fetch_content <url>` first.** Returns clean reader-mode text/markdown for nearly every well-formed page (and handles PDFs and JSON). Try it before anything else.
packages/subagents/agents/codebase-online-researcher.md:40:2. **Check `/llms.txt`.** Many modern docs sites publish an AI-friendly index at `/llms.txt` (spec: [llmstxt.org](https://llmstxt.org/llms.txt)). `fetch_content https://<site>/llms.txt` often links directly to the most relevant pages in plain text, saving a round-trip through the full site.
packages/subagents/agents/codebase-online-researcher.md:4:tools: read, search, find, ls, bash, web_search, fetch_content, get_search_content, todo
packages/subagents/agents/codebase-online-researcher.md:52:| **Conceptual**        | "How do I use X?", "Best practice for Y?"       | `web_search` + `fetch_content` on README/docs               |
packages/subagents/agents/codebase-online-researcher.md:53:| **Implementation**    | "How does X implement Y?", "Show me the source" | `fetch_content` (clone) + `search`/`read` + permalinks        |
packages/subagents/agents/codebase-online-researcher.md:59:**Conceptual.** Batch these in one turn: `web_search` for recent articles or discussions, plus `fetch_content` on the library's GitHub repo URL to clone and check README/docs/examples. Synthesize web results + repo docs and cite official documentation alongside relevant source files.
packages/subagents/agents/codebase-online-researcher.md:63:1. `fetch_content` the GitHub repo URL — this clones it locally to `/tmp/atomic-github-repos/<owner>/<repo>` and returns the file tree.
packages/subagents/agents/codebase-online-researcher.md:69:Batch the initial calls (`fetch_content` to clone + `web_search` for recent discussions) in one turn, then dig into the clone with `search`/`read` once it's available.
packages/subagents/agents/debugger.md:17:- `fetch_content <url>` — the `pi-web-access` fetch tool returns reader-mode text/markdown for URLs (HTML, JSON, PDFs, GitHub issues/PRs, npm, arXiv, RSS, Reddit, Stack Overflow, etc.). Prefer it over a real browser when you only need page content.
packages/subagents/agents/debugger.md:22:- PREFER `fetch_content <url>` for static content. Only reach for the `playwright-cli` skill when you need JS execution, authentication, or interactive page actions.
packages/subagents/agents/debugger.md:46:1. **`fetch_content <url>` first.** The fetch tool returns clean reader-mode text/markdown for HTML, GitHub issues/PRs, Stack Overflow, npm, arXiv, RSS, Wikipedia, Reddit, JSON endpoints, and PDFs — no browser needed.
packages/subagents/agents/debugger.md:47:2. **Check `/llms.txt`.** Many modern docs sites publish an AI-friendly index at `/llms.txt` (spec: [llmstxt.org](https://llmstxt.org/llms.txt)). Try `fetch_content https://<site>/llms.txt` before anything else; it often links directly to the most relevant pages in plain text.
packages/subagents/agents/debugger.md:48:3. **`Accept: text/markdown` header.** Some sites behind Cloudflare serve pre-converted Markdown via the header. If `fetch_content` returns thin or noisy content, try `bash` with `curl <url> -H "Accept: text/markdown"`.
packages/subagents/agents/debugger.md:4:tools: read, edit, write, search, find, ls, bash, web_search, fetch_content, get_search_content, intercom, contact_supervisor, todo
packages/subagents/agents/debugger.md:82:- Use the web research order above (`fetch_content <url>` → `/llms.txt` → `Accept: text/markdown` → playwright-cli) to look up external library docs, error messages, Stack Overflow threads, and GitHub issues
packages/subagents/agents/worker.md:7:tools: read, edit, write, search, find, ls, bash, web_search, fetch_content, get_search_content, intercom, contact_supervisor, todo
packages/subagents/prompts/parallel-handoff-plan.md:41:- Use `fetch_content` first, then `/llms.txt`, then `Accept: text/markdown`, and only fall back to `browser` when JS execution or auth is required.
packages/workflows/builtin/classify-and-act-runner.ts:39:  if (normalized.includes("research")) return ["read", "web_search", "fetch_content"];
packages/workflows/builtin/goal-prompts.ts:104:  "fetch_content",
packages/workflows/builtin/shared-prompts.ts:31:    "Use available video/file tooling such as `fetch_content` on the local video path with a prompt focused on whether the recording proves the required user scenario, or inspect representative frames/metadata when full video analysis is unavailable.",

## research/web/
packages/subagents/agents/codebase-online-researcher.md:151:2. **Check the local cache first**. Look in `research/web/` for existing documents on the topic. If a recent (still-relevant) copy exists, cite it before re-fetching.
packages/subagents/agents/codebase-online-researcher.md:21:- ALWAYS check `research/web/` for a recent cached copy before fetching anything new.
packages/subagents/agents/codebase-online-researcher.md:277:- Check `research/web/` for an existing copy before fetching anything new.
packages/subagents/agents/codebase-online-researcher.md:284:- Persist any high-value fetch to `research/web/` so it does not need to be re-fetched next time.
packages/subagents/agents/codebase-online-researcher.md:301:Remember: you are the user's expert guide to technical research. Lean on `fetch_content` first with the `/llms.txt` → `Accept: text/markdown` → `playwright-cli` fallback chain to efficiently pull authoritative content, clone open-source repos when implementation evidence is needed, store anything reusable under `research/web/`, and deliver comprehensive, up-to-date answers with exact citations and GitHub permalinks. Answer directly — skip preamble like "I'll help you with…" and go straight to findings.
packages/subagents/prompts/parallel-handoff-plan.md:17:- `codebase-online-researcher` — required whenever the request mentions external projects, libraries, docs, APIs, recent changes, or best-practice guidance. It web-searches, fetches authoritative sources, and persists keepers under `research/web/`.
packages/subagents/prompts/parallel-handoff-plan.md:42:- Persist any high-value fetch to `research/web/<YYYY-MM-DD>-<topic>.md`.
packages/subagents/prompts/parallel-research.md:37:- Time-sensitive topics: have the online researcher prefer 2026/2025 sources and persist findings to `research/web/`.

## progress.md
packages/subagents/agents/worker.md:30:- keep `progress.md` accurate when asked to maintain it

## git status --porcelain
packages/workflows/builtin/goal-orchestrator-prompts.ts:21:  "Unless the objective or acceptance criteria explicitly forbid committing, ensure a delegated implementation agent commits the work in the current checkout with a descriptive message before you claim readiness, verify the working tree is clean with the repository's version-control status command (for git: `git status --porcelain`), and include the commit identifier in your receipt. Reviewers treat uncommitted work at readiness as remaining work. Never leave committing as a follow-up action for a later turn.",

## send_to_user

## {task}
packages/workflows/builtin/adversarial-verification-prompts.ts:10:  return `<role>\nYou reduce independent verification reports into one deterministic next action.\n</role>\n\n<objective>\nDecide whether the candidate for ${task} is accepted, rejected, or needs repair.\n</objective>\n\n<artifacts>\nCandidate: ${candidatePath}\nVerifier reports: ${verifierPaths.join(", ")}\n</artifacts>\n\n<decision_rules>\nAccept only when all material rubric requirements have pass evidence. Request repair when findings are actionable and repair budget remains (${repairsCompleted}/${maxRepairs}). Reject when evidence proves the candidate cannot satisfy the task or the repair budget is exhausted. Preserve unresolved blockers verbatim.\n</decision_rules>\n\n<output_format>\nCall structured_output with decision (accept, reject, or repair), rationale, and remaining_work.\n</output_format>`;
packages/workflows/builtin/adversarial-verification-prompts.ts:14:  return `<role>\nYou repair a candidate using independent blocking findings.\n</role>\n\n<objective>\nRepair the candidate for: ${task}\n</objective>\n\n<artifacts>\nRead the current candidate at ${candidatePath} and reducer report at ${reviewPath}.\n</artifacts>\n\n<requirements>\nAddress every actionable blocker, rerun relevant validation, and retain valid prior work. Do not dismiss a finding without contrary evidence.\n</requirements>\n\n<output_format>\nA complete replacement candidate with repair summary, evidence, validation, and remaining risks.\n</output_format>`;
packages/workflows/builtin/adversarial-verification-prompts.ts:2:  return `<role>\nYou produce a candidate solution for independent verification.\n</role>\n\n<objective>\n${task}\n</objective>\n\n<requirements>\nComplete the task, preserve concrete evidence, and state every validation performed. Do not claim success without observable support.\n</requirements>\n\n<output_format>\nA self-contained candidate with actions taken, evidence, validation, and remaining risks.\n</output_format>`;
packages/workflows/builtin/adversarial-verification-prompts.ts:6:  return `<role>\nYou are an independent adversarial verifier. Find blockers; do not rewrite the candidate.\n</role>\n\n<objective>\nVerify the candidate against the task and rubric. Task: ${task}\n</objective>\n\n<artifacts>\nRead the complete candidate at ${candidatePath} and rubric at ${rubricPath}.\n</artifacts>\n\n<requirements>\nTest important claims where practical. A pass requires concrete evidence for every rubric item. Report precise blocking findings; absence of evidence is not evidence of correctness.\n</requirements>\n\n<output_format>\nCall structured_output with verdict (pass or fail), evidence, and blocking_findings.\n</output_format>`;
packages/workflows/builtin/generate-and-filter-prompts.ts:10:  return `<role>\nYou independently judge the filtered shortlist against the explicit rubric.\n</role>\n\n<objective>\nReturn at most ${shortlistSize} ranked candidate paths that best satisfy: ${task}\n</objective>\n\n<artifacts>\nRead the filter report at ${filterPath} and every candidate path it references.\n</artifacts>\n\n<rubric>\nCheck task fit, feasibility, evidence, distinctiveness, and material risk. Do not restore a duplicate merely because it is phrased differently.\n</rubric>\n\n<output_format>\nCall structured_output with shortlist and rationale.\n</output_format>`;
packages/workflows/builtin/generate-and-filter-prompts.ts:14:  return `<role>\nYou present a concise, actionable final shortlist.\n</role>\n\n<objective>\nSummarize the selected candidates for: ${task}\n</objective>\n\n<artifact>\nRead the authoritative selection at ${decisionPath}; follow its order and do not add candidates.\n</artifact>\n\n<output_format>\nRanked markdown shortlist with candidate path, differentiator, evidence, tradeoffs, and recommended next evaluation.\n</output_format>`;
packages/workflows/builtin/generate-and-filter-prompts.ts:2:  return `<role>\nYou independently generate candidate ${ordinal}; do not imitate or assume other candidates.\n</role>\n\n<objective>\n${task}\n</objective>\n\n<requirements>\nProduce one distinct, concrete candidate. Explain its value, constraints, risks, and how it can be evaluated.\n</requirements>\n\n<output_format>\nA self-contained candidate artifact with title, proposal, rationale, risks, and evaluation evidence.\n</output_format>`;
packages/workflows/builtin/generate-and-filter-prompts.ts:6:  return `<role>\nYou deduplicate and filter independently generated candidates.\n</role>\n\n<objective>\nSelect at most ${shortlistSize} strongest candidates for: ${task}\n</objective>\n\n<artifacts>\nRead every candidate: ${candidatePaths.join(", ")}\n</artifacts>\n\n<rubric>\nFirst collapse substantively equivalent candidates. Then score fit to the task, feasibility, evidence, distinctiveness, and risk. Near-duplicates must not gain weight by repetition. Record every discarded candidate and a concrete reason.\n</rubric>\n\n<output_format>\nCall structured_output with shortlist (candidate artifact paths in ranked order) and discarded entries containing path and reason.\n</output_format>`;

## {previous}
packages/subagents/prompts/parallel-context-build.md:14:Use one phase such as `phase: "Context build"` for the parallel tasks so async status is readable. A later synthesis step can reference specific outputs with `{outputs.requestScope}`, `{outputs.codebasePatterns}`, and `{outputs.validationRisks}` instead of relying only on `{previous}`.
packages/subagents/prompts/parallel-handoff-plan.md:31:Use phases such as `Research`, `Local context`, and `Synthesis` so async status is readable. Prefer `{outputs.externalReference}`, `{outputs.localContext}`, and `{outputs.implementationStrategy}` in the synthesis task when those specific inputs are available; keep `{previous}` only when the whole parallel fan-in summary is the desired input.
packages/workflows/builtin/open-claude-design-phases.ts:263:      ["final_design_summary", "{previous}"],

## {chain_dir}

## {item}

## {outputs.name}

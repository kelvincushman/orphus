# Issue 1445 review findings re-check — iteration 2

Validation run in the current worktree:

- `bun run typecheck` -> exit 2.
- `bun run check:file-length` -> exit 1; current count is 26 files over 500 physical lines.
- `git status --short --untracked-files=all` still shows referenced extracted helper modules as `??`.

| finding | applies? | evidence | exact implementation change to resolve |
|---|---:|---|---|
| sdk-exports/sdk.ts missing exports/imports | Yes | `bun run typecheck` still reports `packages/coding-agent/src/core/sdk-exports.ts(33,3)` through `(46,3)` as `TS2304 Cannot find name` for the tool factory/export names. `packages/coding-agent/src/core/sdk-exports.ts:31-46` uses a bare `export { ... }` block without importing those bindings. `packages/coding-agent/src/core/sdk.ts:43-56` still imports those same tool factory names and typecheck reports them unused (`TS6133`). `packages/coding-agent/src/core/sdk.ts:205` and `:219` still refer to `ThinkingLevel` without importing it (`TS2304`). Typecheck also currently reports `sdk.ts:452` and `:465` from the unresolved `thinkingLevel` typing path. | In `packages/coding-agent/src/core/sdk-exports.ts:31-46`, replace the bare local export block with a source re-export from `./tools/index.ts` for `withFileMutationQueue`, `STRUCTURED_OUTPUT_TOOL_NAME`, and the listed `create*` helpers. In `packages/coding-agent/src/core/sdk.ts:1-4`, add `type ThinkingLevel` to the `@earendil-works/pi-agent-core` import. In `sdk.ts:36` and `:43-56`, remove the unused `ResourceLoader` type import and unused tool factory imports, keeping only names that `sdk.ts` uses directly (for example `defaultToolNames`). Re-run typecheck to confirm the `thinkingLevel` calls at `sdk.ts:452`/`:465` are narrowed after the type import. |
| agent-management helper imports | Yes | `bun run typecheck` still reports `packages/subagents/src/agents/agent-management.ts(161,23): Cannot find name 'allAgents'`, `:223`/`:283`+ `Cannot find name 'hasKey'`, and `:239`/`:360` `Cannot find name 'unknownChainAgents'`. The import list at `packages/subagents/src/agents/agent-management.ts:19` imports many helpers but omits those three. The extracted helpers exist in `packages/subagents/src/agents/agent-management-helpers.ts:29` (`hasKey`), `:52` (`allAgents`), and `:103` (`unknownChainAgents`). Typecheck also flags unused imports in `agent-management-helpers.ts:1-5` and unused `parseCsv`/`parseTools` in `agent-management.ts:19`. | Add `allAgents`, `hasKey`, and `unknownChainAgents` to the import from `./agent-management-helpers.ts` at `packages/subagents/src/agents/agent-management.ts:19`. Remove unused `parseCsv` and `parseTools` from that same import unless they become used. In `packages/subagents/src/agents/agent-management-helpers.ts:1-5`, drop the dead `fs`, `path`, `CONFIG_DIR_NAME`, `defaultInheritProjectContext`, `defaultInheritSkills`, and `defaultSystemPromptMode` imports. Also ensure the helper file itself is tracked (see untracked helper row). |
| authoring-contract-ui missing workflow child types | Yes | `bun run typecheck` still reports `packages/workflows/src/shared/authoring-contract-ui.ts(99,15): Cannot find name 'WorkflowRunChildOptions'` and `:100` `Cannot find name 'WorkflowChildResult'`. The type import block at `packages/workflows/src/shared/authoring-contract-ui.ts:5-27` imports `WorkflowDirectTaskItem`, which typecheck reports unused, but omits the two used child workflow types. The missing types are exported from `packages/workflows/src/shared/authoring-contract-stage.ts:400` and `:422`. | In `packages/workflows/src/shared/authoring-contract-ui.ts:5-27`, add `WorkflowRunChildOptions` and `WorkflowChildResult` to the type import from `./authoring-contract-stage.js`; remove `WorkflowDirectTaskItem` from that import unless it is reintroduced elsewhere. |
| package-manager-cli `UpdateTarget` export and dead import | Yes | `bun run typecheck` still reports `packages/coding-agent/src/package-manager-cli.ts(29,85): TS2459 Module './package-manager-cli-parser.ts' declares 'UpdateTarget' locally, but it is not exported`. `packages/coding-agent/src/package-manager-cli-parser.ts:6` declares `type UpdateTarget = ...` without `export`. Typecheck also reports `packages/coding-agent/src/package-manager-cli.ts(6,2): 'CONFIG_DIR_NAME' is declared but its value is never read`; line 6 imports it from `./config.ts`. | In `packages/coding-agent/src/package-manager-cli-parser.ts:6`, change the declaration to `export type UpdateTarget = ...`. In `packages/coding-agent/src/package-manager-cli.ts:4-13`, remove `CONFIG_DIR_NAME` from the `./config.ts` import. Ensure `package-manager-cli-parser.ts` is tracked (see untracked helper row). |
| remaining file-length violations | Yes | `bun run check:file-length` still exits 1. Current output is improved from the earlier 37-file report but still says `File length check failed: 26 files exceed 500 physical lines.` Representative remaining examples include `packages/coding-agent/test/agent-session-auto-compaction-queue.test.ts` 754, `test/unit/bash-command-policy.test.ts` 715, `test/unit/workflow-failures.test.ts` 711, and `test/integration/runtime-tunables.test.ts` 700. | Continue splitting/trimming the 26 reported authored files until `bun run check:file-length` reports zero violations. Only add file-length exclusions for documented generated/vendor files or explicit generated markers; do not exclude ordinary authored source/tests. |
| untracked `config-write-utils` / extracted helper modules | Yes | `git status --short --untracked-files=all` still shows referenced helper modules as untracked, including `?? packages/mcp/config-write-utils.ts`. This is broader than the original MCP-only finding: tracked files currently import untracked helpers, e.g. `packages/mcp/config.ts:8-9` imports/exports `./config-write-utils.ts`; `packages/mcp/ui-server.ts:14` imports `./ui-server-http.ts`; `packages/coding-agent/src/core/auth-storage.ts:19` imports `./auth-storage-backends.ts`; `packages/coding-agent/src/core/sdk.ts:59-62` imports/exports `./sdk-types.ts` and `./sdk-exports.ts`; `packages/coding-agent/src/migrations.ts:75` imports `./migrations-config-values.ts`; `packages/coding-agent/src/modes/interactive/components/config-selector.ts:10` imports `./config-selector-list.ts`; `packages/coding-agent/src/package-manager-cli.ts:29` imports `./package-manager-cli-parser.ts`; `packages/subagents/src/agents/agent-management.ts:19` imports `./agent-management-helpers.ts`; `packages/subagents/src/agents/skills.ts:7-8` imports/exports `./skills-paths.ts`; `packages/workflows/src/shared/persistence-restore.ts:7` imports `./persistence-restore-helpers.js`; `packages/workflows/src/tui/inline-form-editor.ts:91` imports `./inline-form-editor-text.js`; and `packages/workflows/src/tui/workflow-attach-pane.ts:51` imports `./workflow-attach-pane-types.js`. | Track the extracted helper modules that are intended to be part of the split (stage/commit `packages/coding-agent/src/core/auth-storage-backends.ts`, `sdk-exports.ts`, `sdk-types.ts`, `packages/coding-agent/src/migrations-config-values.ts`, `packages/coding-agent/src/modes/interactive/components/config-selector-list.ts`, `packages/coding-agent/src/package-manager-cli-parser.ts`, `packages/mcp/config-write-utils.ts`, `packages/mcp/ui-server-http.ts`, `packages/subagents/src/agents/agent-management-helpers.ts`, `packages/subagents/src/agents/skills-paths.ts`, `packages/workflows/src/shared/persistence-restore-helpers.ts`, `packages/workflows/src/tui/inline-form-editor-text.ts`, and `packages/workflows/src/tui/workflow-attach-pane-types.ts`). If any split is not intended, remove the corresponding imports and inline/revert the code instead. |

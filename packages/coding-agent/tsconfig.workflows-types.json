{
	"//": "Issue #1208: emit the lean @bastani/workflows authoring type surface as .d.ts so the @bastani/atomic/workflows exports condition resolves to declarations (never raw .ts, which would leak the SDK's internal type errors into consumers). Declaration-only; no JS is emitted. This tsconfig lives in coding-agent on purpose — packages/workflows must NOT gain a build step.",
	"extends": "../../tsconfig.base.json",
	"compilerOptions": {
		"noEmit": false,
		"emitDeclarationOnly": true,
		"declaration": true,
		"declarationMap": false,
		"sourceMap": false,
		"inlineSources": false,
		"removeComments": false,
		"rootDir": "../workflows/src",
		"outDir": "./dist/builtin/workflows/src"
	},
	"include": ["../workflows/src/authoring.ts"],
	"exclude": ["node_modules", "dist"]
}
